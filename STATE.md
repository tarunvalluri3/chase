# STATE.md — Chase Progress Tracker

## How to resume (read this first)

At the start of any session, Claude Code reads `CLAUDE.md` then `STATE.md` before doing anything else. Work only proceeds on a phase that has been explicitly **approved** by the user in this session or a prior one — never on a phase whose status below is merely "Prompt ready." If unsure which phase is current, ask.

---

## Current Phase

**UI redesign + Reminder/Repeat features (outside the phase sequence, explicitly approved by the user, overriding the normal phase-prompt gate for this one case — logged in `client/DESIGN.md` §12's approved-decisions table).** Two parts:

**1. Full dark-navy UI redesign, superseding `DESIGN.md` v2 ("The Ledger," light theme) with v3 ("Dark Navy"):** user supplied six reference screenshots + a detailed brief; confirmed via `AskUserQuestion` before starting that this fully supersedes v2 (not an incremental tweak) and that `DESIGN.md` itself should be rewritten as the new authoritative reference. **Tokens** (`client/src/styles/tokens.css`): every color repointed to the new dark-navy/ocean-blue palette while keeping semantic alias names stable (`--color-active`, `--color-danger`, `--color-priority-*`, etc.) to avoid touching ~15 consumer files — the one real bug this repoint strategy caught: v2's `--color-priority-low` aliased `--color-pine` directly (pine doubled as both brand accent and low-priority green in v2's palette), which would have silently turned Low-priority rails brand-blue instead of green once pine was repointed to the new brand blue; fixed by aliasing `--color-priority-low` to `--color-moss` (success green) instead, decoupling brand identity from priority semantics. Five statuses map onto only four brief-defined semantic colors — `INCOMPLETE`/`DELETED` got two distinct new muted-neutral tones rather than reusing warning/danger, so a confirmed-not-done or deleted task never implies the severity of an active warning or destructive action (a deliberate call, not a default). Radius/spacing scale values updated (8/12/16/20/24px radius; 24px screen gutter, 12px card gap, 20px card padding — up from v2's 16px on both). Fonts: Inter only (`@fontsource-variable/inter`), replacing Literata+Manrope+JetBrains Mono entirely; every `font-serif`/`font-mono` className usage across the app (a dozen+ files) was found via full-repo grep and either dropped (serif headers fall back to the inherited sans body font) or replaced with a new `.font-tabular` utility (`font-variant-numeric: tabular-nums`) for numeral alignment (stat counts, chart axis labels, filter/tab counts, timestamps, durations) — confirmed via a final `grep -rnE "#[0-9a-fA-F]{3,8}"` and a `font-serif|font-mono` sweep that both return zero matches outside `tokens.css` itself. Gradients removed from the FAB (now flat `--color-brand`) and the Profile avatar, per the brief's explicit "no gradients on functional chrome" rule; the brand mark asset was also swapped from `chase-mark-on-light.svg` to the already-existing `chase-mark-on-dark.svg` variant in all four places it's used (`Sidebar`, `Landing`, `AuthBackdrop`), caught by checking `client/public/brand/README.md` rather than assuming. **Structural (not just token) changes, each confirmed against the actual current code before assuming a pure restyle would do:** `BottomNav.jsx` — replaced the v2 `layoutId` capsule-morph (only the active tab showed a label) with always-visible icon+label on all four tabs plus a static colored background chip on the active one, matching the reference screenshots and the brief's "no gratuitous animation" rule; `StatTile.jsx`/`StatusCounts.jsx` — restructured from v2's thin-rule-divided ledger strip into icon-bubble stat cards (a lucide icon in a `color-mix()`-tinted circle above the numeral), reused by both Home's 5-cell status row and Profile's 2-cell stat row (icon-less there); `TaskCard.jsx` — added a decorative-only leading checkbox circle (unfilled ACTIVE / filled+check COMPLETED) matching the screenshots, deliberately not wired to any handler so it can never create a second, conflicting completion path alongside the existing Complete/Delete buttons. `Button.jsx` had a real pre-existing bug fixed while it was being touched: it hardcoded Tailwind's literal `rounded-md` (6px) instead of the token-based `rounded-(--radius-md)`, so it was never actually themed off the radius token at all. **New, real, client-side-only features added to the Tasks-list toolbar** (sort by due/priority/newest, list/grid view toggle) — state lives in `TasksProvider` (`sortBy`/`viewMode`, the latter `localStorage`-persisted), a new `sortTasks()` pure function in `lib/taskStats.js` re-orders the already-fetched section array, no new API parameter. The Notifications sheet also gained a real (not decorative) All/Tasks/Reminders/System tab filter over its existing feed data, client-side only. `clerkAppearance.js` retokenized (dropped `font-serif`, fixed `text-canvas`→`text-white` on the primary button — canvas flipped from light to dark, so the old "canvas-colored text on an accent button" trick would have rendered near-invisible dark-on-blue text if left unchanged; the same `text-canvas` bug was caught and fixed in `Button.jsx`'s primary variant and Profile's avatar for the same reason). **Verification:** full frontend suite **127/127 passing** (up from 126 — two new `StatTile` tests for the icon-bubble color, one dashboard test rewritten since the label no longer carries the status color directly, the icon bubble does), `npm run build` succeeds (same pre-existing >500KB chunk-size warning as every phase since 15), `npx oxlint src` shows only the same six pre-existing warning shapes, no new findings. A temporary Playwright pass (installed with `--no-save`, `window.__TEMP_BYPASS_AUTH__` wired into `Root.jsx`/`ProtectedRoute.jsx` and fully reverted after — confirmed via `git status` showing zero diff on both files — full network-level mocking of `/api/tasks*`/`/api/notifications*`/`/api/analytics/*` with a seven-task fixture spanning every status) confirmed at both **390×844 mobile** (exercises `BottomNav`) and **1280×900 desktop** (exercises `Sidebar`): Home's icon-bubble stat row, Due soon, and Recent activity; the Tasks list with working sort/grid-view controls and Active/Completed tabs; task detail showing the new Reminder/Repeat indicators; Profile's avatar/stats/settings rows; the New Task sheet's Reminder toggle and Repeat segmented control; and the Notifications sheet's tab filter — all render correctly against the new dark palette with zero console errors on the mocked routes (the only console errors seen were expected `ERR_CONNECTION_REFUSED` on the unmocked work-session `/sessions` endpoint, not a redesign regression). Two minor, disclosed-not-fixed polish gaps from that pass: the 5-cell Home stat row overflows narrow 390px viewports (already `overflow-x-auto`, consistent with v2's same behavior, not a new regression) and the Notifications sheet's tab row is visually tight against "Mark all read" at 390px (functional, not broken — tabs scroll). `client/DESIGN.md` fully rewritten as v3 (13 sections mirroring v2's structure so in-code `// DESIGN.md §x` cross-references stay valid), including a new §12 approved-decisions table logging every judgment call above.

**2. Reminder + Repeat (recurring tasks) — real, backend-wired new features, explicitly approved by the user overriding the normal "draft a phase prompt first" gate** (`PHASES.md` states new features normally require that process; the user confirmed proceeding anyway for this session). Migration `server/supabase/migrations/20260821120000_add_reminder_and_repeat.sql` (three new nullable/defaulted columns on `tasks`: `reminder_enabled boolean default false`, `repeat_rule text default 'NONE'` with a check constraint against `NONE|DAILY|WEEKLY|MONTHLY`, `repeat_group_id uuid` — **not yet applied by the user through the Supabase SQL editor as of this session's end**, confirmed by a direct Supabase query still returning "column does not exist"; backend tests for these features cannot pass until it is). **Reminder**: a per-task boolean opt-in filter on the *existing* global `DEADLINE_REMINDER_HOURS_BEFORE` sweep (`jobs/notificationScheduler.js`'s `findActiveDueWithin` gained one `.eq('reminder_enabled', true)` line) — deliberately not a new per-task custom offset, since no such setting exists anywhere in the app today (verified by reading `Profile.jsx` directly before assuming one did). **Repeat**: new `services/recurrenceService.js` (`maybeSpawnNextOccurrence`, mirroring `notificationService.js`'s self-contained/never-throws shape), called as a pure side effect appended after `tasksService.completeTask` and `resolveMissedTask`'s `COMPLETED` branch only — never on `MISSED`, `INCOMPLETE`, or `DELETED`, and never touching the state-machine transition guards themselves. Completing a repeating task spawns a **brand-new** `ACTIVE` task (never mutates the completed one), with its deadline advanced from the task's **original** deadline (confirmed via `AskUserQuestion`: anchored to the original schedule, not the actual completion time, so "every Monday" stays Monday regardless of when it was actually finished) by one calendar interval (hand-rolled UTC date math, no new date-library dependency, consistent with the project's existing house rule). A `repeat_group_id` (UUID, generated app-side in `tasksService.createTask` only on true series-starting creations) threads every occurrence in a chain together for future analytics use, with no series-wide edit/delete — each spawned occurrence is a fully independent row, explicitly scoped out rather than silently expanded. Frontend: `TaskForm.jsx` gained a real Reminder switch and a 4-option Repeat segmented control (styled like the existing Priority control), wired into the create/edit payload; new `components/tasks/repeatConfig.js` (label-only, no color semantics, mirroring `priorityConfig.js`'s shape); `TaskDetail.jsx` shows a bell/repeat indicator when either is set. **Testing:** `server/tests/recurrence.test.js` (new — daily/weekly/monthly deadline math, `NONE` spawns nothing, resolve-missed→COMPLETED also spawns while →INCOMPLETE/DELETE do not, cross-user isolation, chain continuation across two generations, invalid-`repeat_rule` 400) and `server/tests/notifications.scheduler.test.js` extended (two pre-existing reminder tests updated to pass `reminder_enabled: true` explicitly, since reminders are now opt-in rather than unconditional for every due-soon task — without this update they would have started failing against the new opt-in filter; two new tests for the opt-out case and the create/PATCH round-trip) — **not yet run against a real database this session**, blocked on the pending migration above.

**Closed out in a follow-up to this same session, after the user applied the migration:** confirmed live via a direct Supabase query (`tasks.reminder_enabled` etc. now exist). Full backend suite run: **182/182 passing** on a clean run. Along the way, two pre-existing, disclosed-not-introduced issues surfaced and were resolved/characterized: (1) `tests/workSessions.test.js`'s "multiple closed segments plus one open segment sum correctly" — a real, deterministic test bug unrelated to this session's changes (confirmed via `git diff` showing zero prior touches to any work-session file): `workSessionsService.getSummary` floors elapsed time to whole seconds (`Math.floor(totalMs / 1000)`), but the test only slept 50ms+50ms, so `totalSeconds` was always going to floor to 0 — fixed by bumping both sleeps to 600ms (user confirmed via `AskUserQuestion` before this out-of-scope-but-trivial fix was made). (2) A second `workSessions.test.js` test (the 6-second real-deadline auto-close-on-MISSED test) failed twice then passed once across three repeated full-suite runs, but passed 100% reliably every time it was run in isolation or paired only with the two new Reminder/Repeat test files — the exact same "shared, non-dedicated test Supabase project" cross-worker-contention class already disclosed since Phase 7 and hit again in Phase 19 (that time, a genuine latency-widened race condition in `tasksService.js` itself was found and fixed; this time, confirmed by reading `maybeTransitionToMissed`/`autoCloseOpenSession` — both are correct, unchanged code, and the flake is purely a function of `recurrence.test.js` adding more concurrent DB load to an already-known-fragile shared resource, not a logic defect). Left as disclosed, pre-existing infrastructure debt per the same standing conclusion every prior phase reached (a dedicated test Supabase project would remove it entirely) — not fixed, since there is no code-level bug to fix. **This entire redesign + Reminder/Repeat effort is now fully verified and closed**, pending only the user's decision on whether/when to deploy.

**Bug fix (outside the phase sequence, done alongside Phase 20 at the user's request):** desktop (>=960px) had no way to create a task once any task existed. Investigated before changing anything, per the user's own preference: `AppLayout.jsx`'s desktop breakpoint (`Sidebar`, DESIGN.md §6.3) is real, deliberate, already-shipped chrome — this wasn't "mobile-only by design excluding desktop," it was a genuine gap in already-built desktop support. Root cause: the only create-task entry point once a user has any tasks is the FAB inside `BottomNav`, and `BottomNav` (including the `Sheet`+`TaskForm` it used to render) was wrapped in `min-[960px]:hidden` — a `display:none` ancestor hides a `position:fixed` descendant regardless, so the create sheet was unreachable at the desktop breakpoint even via direct context calls. Fixed by extracting the create `Sheet` into a new shared `components/tasks/CreateTaskSheet.jsx`, rendered once at the `AppLayout` level (alongside `ToastViewport`/`NotificationsSheet`, outside the hidden wrapper) instead of nested inside `BottomNav`; `BottomNav` now only owns the FAB button. Added a "New task" button to `Sidebar.jsx` (reusing the existing `Button` primitive and the same `openCreateSheet` from `tasksStore` that the FAB and Home's empty-state button already call — no new create logic). `taskFlows.test.jsx`'s existing create-flow test updated to compose `BottomNav`+`CreateTaskSheet` together (matching how `AppLayout` now composes them); a new regression test added confirming the same flow works from `Sidebar`+`CreateTaskSheet`. Full frontend suite: **126/126 passing** (up from 125), build succeeds, lint clean.

**Phase:** 20 — Productivity Analytics 2.0. Done.
**Overall status:** Upgraded Insights from Phase 15's task-only, client-side-aggregated view into a fuller "Productivity Analytics 2.0" dashboard, per `PHASE_20.md`/`prompts/phase-20.md`. **Key architectural decision, flagged for confirmation before implementation and confirmed by the user via `AskUserQuestion`: server-side aggregation** (`PHASE_20.md` §2's recommended default) over extending Phase 15's client-side `analyticsStats.js` — chosen because the new time-tracking metrics need `work_sessions` data across *every* one of a user's tasks, which no endpoint exposed before this phase (every prior `workSessionsRepository`/`workSessionsService` method was task-scoped only), and because summing potentially-unbounded session rows in the browser doesn't fit "use efficient PostgreSQL queries." **New backend layer**, kept fully separate from task CRUD/lifecycle logic: `routes/analytics.js` → `controllers/analyticsController.js` → `services/analyticsService.js` (all aggregation/bucketing math) → existing `tasksRepository`/`workSessionsRepository`, `validation/analyticsSchemas.js`. One endpoint, `GET /api/analytics/summary?range=7d|30d|90d|all` (default `all`, matching Phase 15's previously-unfiltered behavior so nothing regresses), mounted in `app.js`. **The one genuinely new repository capability**, exactly as scoped in `PHASE_20.md` §0/§6: `workSessionsRepository.listByUserSince(userId, since)` — cross-task, `.gte('started_at', since)` applied only when a lower bound exists. `analyticsService.getSummary` fetches a user's full unfiltered task list (`tasksRepository.listByUser`, same convention Phases 14/15 already established) plus range-filtered sessions in parallel, then computes: KPIs (`totalTasks`, `completedTasks`, `missedUnresolvedTasks` — current MISSED snapshot, never range-scoped, since it's a pending checkpoint per `CLAUDE.md` — `missedEverTasks`, `deletedTasks`, `completionRate` = COMPLETED/(COMPLETED+INCOMPLETE) in range, `missedRate` = missed-ever/total-created in range, `deletionRate`, `totalTrackedSeconds`), weekly trend series (`completionTrend` reused as-is from Phase 15's math, **`missedTrend` new** — weekly `missed_at` detection-event count, deliberately independent of eventual resolution and never conflated with "never completed," `incompleteTrend`/`deletedTrend` reused), `priorityBreakdown` (reused, now range-filtered on each terminal task's own timestamp), **`deadlinePerformance` new** — compares `completed_at` directly to `deadline` for every COMPLETED task in range, catching a task completed late while still `ACTIVE` before any lazy `MISSED` transition ran, not just tasks that visibly passed through `MISSED` first, `incompleteReasons`/`deletedReasons` (reused ranking logic), `unresolvedMissed` (reused, deliberately **not** range-filtered — a current snapshot, not history), and the new time-tracking metrics: `timeTrackedTrend` (weekly sum of session durations, `ended_at ?? now()` for an open segment, same math as Phase 19's `getSummary`), `timePerTask` (top 20 by total tracked seconds), `timeVsOutcome` (avg tracked time grouped by the task's current terminal status — COMPLETED/INCOMPLETE/DELETED), `priorityTimeSpent` (avg tracked time grouped by priority). Every weekly-bucket helper (`weekKey`/`weekLabel`/`recentWeekKeys`, Monday-start UTC) is a direct server-side port of Phase 15's client algorithm, not a reinvention, so the reused charts render identically once fed this endpoint's payload. **Frontend:** `apiClient.js` gained `analyticsApi.summary(range)`; new `hooks/useAnalyticsSummary.js` (replaces `Insights.jsx`'s prior `useDashboardTasks` dependency, refetches on both range change and `TasksProvider`'s `version` bump so a task action elsewhere in the app is reflected here too). **Existing Phase 15 chart components preserved and reused, not rebuilt**, per the phase's explicit instruction — `CompletionTrendChart`, `IncompleteTrendChart`, `DeletedTrendChart`, `PriorityBreakdown`, `UnresolvedMissed` had their internal `computeX(tasks)` calls removed and now take the already-aggregated `data`/`breakdown`/`missed` prop directly; all JSX/Recharts/`ChartCard` rendering logic is unchanged. **New chart components** under `components/analytics/`: `KpiRow.jsx` (reuses the existing `LedgerStrip`/`StatTile` primitives from the Home dashboard's `StatusCounts` rather than inventing a second KPI-tile visual language — five tiles: total tasks, completion/missed/deletion rate, time tracked), `DateRangeFilter.jsx` (segmented pill control styled after the existing `FilterRow` selected/unselected treatment, one control driving every section per the phase's "avoid overcrowding" instruction), `StatusComparisonChart.jsx` (one direct completed-vs-missed-vs-deleted snapshot for the selected range, reusing each status's existing color token), `MissedTrendChart.jsx` (amber `--color-review`, matching `UnresolvedMissed`'s existing "a passed deadline is a question, not a verdict" color choice), `DeadlinePerformanceChart.jsx` (two-slice donut sharing the Completed hue at two opacities — reuses the same `--color-chart-completed-resolved` token `CompletionTrendChart` already introduced in Phase 15, no new hue), `TimeTrackedTrendChart.jsx` (area chart, weekly), `TimePerTaskTable.jsx` (a table, not a chart — the one section judged to need more than a single bar-length encoding; each row links to its task via the existing `/tasks/:status/:id` route pattern), `TimeVsOutcomeChart.jsx` and `PriorityTimeChart.jsx` (avg-time bar charts reusing status/priority color tokens respectively). No new colors/hexes introduced anywhere — confirmed via the same `grep -rnE "#[0-9a-fA-F]{3,8}"` sweep every prior phase has run, zero matches. `Insights.jsx` restructured into the `PHASE_20.md` §8 section order (date-range filter and KPI row first, "needs attention" next, task-outcome analytics, then time-tracking analytics last) with all four states (loading/error/empty/loaded); the empty state now also checks `totalTrackedSeconds` (not just task counts) before showing "Nothing to show yet." **Cleanup:** `client/src/lib/analyticsStats.js` deleted — confirmed zero remaining production imports after `Insights.jsx`'s rewrite (only a stale comment cross-reference in two other files, updated to point at the new server-side location instead) — per the project's standing "delete code once genuinely unused rather than leaving it as a compatibility shim" rule. **Testing:** `server/tests/analytics.test.js` (new — auth requirement, invalid-`range` 400, a brand-new user's fully-zeroed payload shape, task-derived KPI/rate/reason/priority/deadline-performance math against real seeded tasks across COMPLETED/DELETED/MISSED→INCOMPLETE/ACTIVE, time-tracking metrics against real `work_sessions` rows including one auto-closed by Complete, and cross-user isolation for both tasks and sessions) — **12/12 passing**, full backend suite **169/169 passing** (up from 157 at the close of Phase 19). Frontend: `components/analytics/__tests__/analytics.test.jsx` updated for the five reused components' new prop shapes plus new tests for every new component (`KpiRow`, `DateRangeFilter`, `StatusComparisonChart`, `MissedTrendChart`, `DeadlinePerformanceChart`, `TimeTrackedTrendChart`, `TimePerTaskTable`, `TimeVsOutcomeChart`, `PriorityTimeChart`); `test/integration/dashboardInsights.test.jsx`'s Insights block rewritten to mock `analyticsApi.summary` instead of `tasksApi.list`, plus a new test confirming the date-range filter triggers a refetch with the correct `range` query value — **full frontend suite 125/125 passing** (up from 112). `npm run build` succeeds (same pre-existing Recharts chunk-size warning as every phase since 15); `npx oxlint src` shows only the same pre-existing warning shapes, no new findings. **Not verified live:** unlike most prior frontend phases, no temporary Playwright pass against a running dev server was performed this session (the `window.__TEMP_BYPASS_AUTH__` mechanism those passes have relied on since Phase 10 doesn't persist between phases and wasn't re-added here) — confidence instead rests on the passing jsdom/RTL integration tests (which do exercise the real chart components against realistic mocked payloads) and a clean production build; real-browser rendering of the new Recharts components (donut chart, area chart, the new table) and mobile-viewport layout were not independently re-confirmed, and this is a real gap relative to this project's established verification bar, disclosed rather than silently skipped. Also not verified: an actual signed-in session hitting the real backend end-to-end (no real Clerk credentials non-interactively, consistent with every prior phase).

**Phase:** 19 — Time Tracking. Done.
**Overall status:** Built Start/Pause/Resume/Stop work-session tracking on top of the finished task model, per `PHASE_19.md`/`prompts/phase-19.md` — backend-only, no frontend, matching how Phases 0–8 preceded Phases 9+ for the original task model. **Segment-based data model, confirmed as the one open judgment call before implementation began** (`PHASE_19.md` §5 flagged auto-close vs. leave-open for explicit confirmation): the user chose **auto-close**. New `work_sessions` table (migration `20260819160000_add_work_sessions.sql`, applied by the user through the Supabase SQL editor, same manual process as every prior migration) — one row per continuous work stretch, `end_reason` in `PAUSED | STOPPED | AUTO_STOPPED`, a **partial unique index** (`work_sessions_one_open_per_task ... where ended_at is null`) enforcing "at most one open segment per task" at the database level, not just in the service layer. `user_id` is denormalized onto the table (same reasoning `CLAUDE.md` already applies to `tasks`) so every query filters directly without a join. FKs use `on delete cascade` (a deliberate small deviation from `PHASE_19.md`'s literal SQL, added for consistency with every other Phase-17/18 table's FK convention and because the test suite's `cleanupUser` hard-deletes rows). **New layer:** `routes/workSessions.js` (an `express.Router({ mergeParams: true })` mounted at `/api/tasks/:id/sessions` from within `routes/tasks.js`, so `:id` — the task id — flows through from the parent router), `controllers/workSessionsController.js`, `services/workSessionsService.js`, `repositories/workSessionsRepository.js`, `validation/workSessionSchemas.js` (re-exports `taskSchemas.js`'s `idParamSchema` — no body schemas needed, since none of the six endpoints accept client-writable fields). **Six endpoints:** `POST /start|pause|resume|stop`, `GET /` (full chronological segment history), `GET /summary` (`{ totalSeconds, isRunning, currentSessionStartedAt }`, computed live by summing closed segments plus elapsed time on any still-open one — never a stored value). Ownership is enforced by routing every session action through `tasksService.getTask` first (a deliberate, one-directional-in-practice circular import between `workSessionsService.js` and `tasksService.js` — safe under ESM since both sides only invoke the other's export from inside an async function body, never at module-evaluation time), which gives session routes the exact same 404-for-foreign-or-nonexistent-task behavior every other task route already has, for free. **Lifecycle rules exactly per `PHASE_19.md` §4:** Start/Resume require the task ACTIVE and no already-open segment; Resume additionally requires the task's most recent segment to have ended specifically with `PAUSED` (a 409, not 400, matching `CLAUDE.md`'s existing invalid-transition status code); Pause/Stop require an open segment to exist. A race between two concurrent Start/Resume calls is caught two ways: the pre-check in the service, and — if two requests still interleave past that check — the partial unique index itself, whose Postgres `23505` violation `workSessionsService.createSegment` maps onto the identical 409 rather than letting a raw DB error leak. **Auto-close (`end_reason = 'AUTO_STOPPED'`) wired into the three call sites `PHASE_19.md` specifies:** `tasksService.completeTask`, `tasksService.deleteTask`, and the internal `maybeTransitionToMissed` — each calls `workSessionsService.autoCloseOpenSession(taskId)` (a no-op when there's no open segment) immediately after its own status write. **Known, disclosed scope boundary, not silently expanded:** the two *bulk* missed-sweep paths — `POST /api/tasks/sweep-missed` (`tasksRepository.sweepMissed`) and the notification scheduler's cross-user `sweepAllUsersMissed` — write directly via the repository and bypass `maybeTransitionToMissed` entirely, so a session left open on a task swept through either of those bulk paths is **not** auto-closed by this phase's three call sites; `PHASE_19.md`'s own file list names exactly those three functions, not the bulk sweeps, so this was treated as the approved scope's boundary rather than an oversight to silently patch — worth flagging if it matters before Phase 21 (planned-vs-actual analytics) is built. **A real, pre-existing correctness bug found and fixed during this phase, not scope creep:** `maybeTransitionToMissed`'s write was an unconditional read-then-write with no guard against the task's status having changed in between — a concurrent Complete/Delete that landed first could be silently overwritten back to `MISSED` by a lazy-check request that read the stale `ACTIVE` row first. This always existed, but was latent enough to essentially never manifest before; Phase 19's extra Supabase round-trip in `completeTask`/`deleteTask` (the new `autoCloseOpenSession` call) widened the race window enough that it started intermittently failing pre-existing, unrelated tests (`tasks.lifecycle.test.js`) under full-suite parallel execution — confirmed by running the suite with the new `workSessions.test.js` file excluded (clean) vs. included (intermittent MISSED-overwrite failures), which pointed straight at the added latency rather than at the new test file's own assertions. Fixed with a new guarded repository function, `tasksRepository.transitionToMissedIfActive`, which adds `.eq('status', 'ACTIVE')` to the update and returns `null` (instead of throwing) when the row no longer matched, so `maybeTransitionToMissed` can fall back to re-reading the task's actual current state rather than blindly returning the stale in-memory object. **Testing:** `tests/workSessions.test.js` — lifecycle happy path (Start→Pause→Resume→Stop, correct segment rows and history ordering), multiple full Start→Stop cycles on one task, every invalid-state 409 in `PHASE_19.md` §8 (double-Start, Pause/Stop with nothing open, Resume with no prior segment, Resume after a STOPPED segment, Start on COMPLETED/DELETED/MISSED tasks), the full cross-user ownership battery (all six endpoints 404 for a foreign task id, mirroring `authorization.test.js`'s existing pattern), auto-close on Complete and on Delete, auto-close on the lazy ACTIVE→MISSED transition (a task started with a deadline a few seconds out, then read again after it passes), and summary math (zero-sessions returns a real zeroed object rather than an error; multiple closed segments plus one open segment sum to a positive, correctly-`isRunning` total). `tests/helpers/db.js`'s `cleanupUser` extended to delete `work_sessions` rows before `tasks`. **Full backend suite: 157/157 passing** (up from 137 at the close of Phase 18) — verified clean across repeated full-suite runs after the race-condition fix; one residual, already-disclosed flake class remains (the shared, non-dedicated test Supabase project noted since Phase 7/17 — a *different*, unrelated pre-existing test, `sweep-missed bulk-transitions...`, failed once in three repeated full-suite runs, and passed cleanly every other time and every time run in isolation or paired with just the new file — consistent with cross-worker contention on the shared project, not a Phase 19 regression). **Frontend added in a follow-up to this same phase, at the user's explicit request** ("why no front-end? Update the front-end too") after the backend-only close above — three placement/behavior decisions were confirmed via `AskUserQuestion` before building, matching this project's established pattern for UI judgment calls: controls live on **both** `TaskCard` (compact) and `TaskDetail` (full), the running timer **ticks live** client-side rather than only updating on refetch, and the detail page shows **only the running total**, not segment-by-segment history (that stays backend data, for Phase 21 if/when it's built). New `apiClient.js` `sessionsApi` (`start`/`pause`/`resume`/`stop`/`list`, no separate call to the summary endpoint — see below); `lib/datetime.js` gained `formatDuration` (mono `H:MM:SS`/`MM:SS`); `lib/taskLifecycle.js` gained `sessionErrorMessage`, worded for a session action rather than a task-status transition but following the same §3.3 voice (409 → *"This session changed somewhere else. Refresh and try again."*). **`hooks/useWorkSession.js`** fetches the task's full segment history (`GET /sessions`) once and derives `totalSeconds`/`isRunning`/`isPaused` client-side with the same math as the backend's own `getSummary` — deliberately *not* also calling the separate `GET /sessions/summary` endpoint, since the full list is the only shape that also reveals whether the most recent segment ended `PAUSED` (needed to render Resume vs. Start), which the summary alone doesn't expose. A `setInterval` re-derives the total every second only while a segment is open, so the ticking timer costs no extra network calls. **`components/tasks/TimeTracker.jsx`** renders the controls: `compact` (icon-only buttons + `aria-label`, used on `TaskCard`) vs. full (labeled buttons + a "Time tracked" caption, used on `TaskDetail`); a `readOnly` prop (passed by `TaskDetail` whenever `task.status !== 'ACTIVE'`) shows the total with zero action buttons for a terminal task with prior tracked time, and renders nothing at all if there's no history and nothing to show — matching every other lifecycle action's ACTIVE-only gating without ever showing a button that would just 409. `TaskCard` renders `TimeTracker` compact, ACTIVE-only, directly above the existing Complete/Delete row; `TaskDetail` renders it full-width between the metadata `<dl>` and the reason blocks. `server/API.md` was **not** extended for the six new session endpoints, matching the established precedent that it was frozen at the close of Phase 8 and never updated for Phase 17/18's endpoints either. **Testing:** `TimeTracker.test.jsx` (8 tests, `sessionsApi` mocked) — Start-when-empty, Start→Pause/Stop transition, a PAUSED-ended segment renders Resume not Start, Pause/Stop call the API with the right task id and land back in the correct state, compact mode's icon-only buttons carry an accessible name, `readOnly` renders nothing with no history and renders total-only-no-buttons with history. **Full frontend suite: 112/112 passing** (up from 104 at the close of Phase 18); `npm run build` succeeds (same pre-existing Recharts chunk-size warning as every phase since 15); `npx oxlint src` shows no new findings (same five pre-existing warnings as Phase 18). **Verified live** via the same temporary-Playwright-plus-network-mocking pattern used since Phase 10 (`playwright` installed with `--no-save` and fully removed after; `window.__TEMP_BYPASS_AUTH__` wired into `Root.jsx`/`ProtectedRoute.jsx` and fully reverted after, confirmed via a clean `git status`/`git diff` on both files post-revert — no real Clerk credentials available non-interactively, consistent with every prior phase): Start on a fresh ACTIVE task's card switches to a live-ticking Pause/Stop pair (confirmed the displayed time actually incremented across real waits, not just a snapshot); Pause shows Resume; navigating to the detail page mid-session shows the same paused total under a "Time tracked" label; Resume then Stop from the detail page returns to a startable state; a COMPLETED task with prior tracked history shows the total with zero action buttons. **Not verified live:** an actual signed-in session against the real deployed backend (no real Clerk credentials non-interactively, consistent with every prior phase) and real-device 200% OS text-scaling on the new controls (same unchanged `rem`-based mechanism as every prior frontend phase, not re-verified on a physical device).

**Phase:** 18 — Push Notifications & In-App Feed. Done.
**Overall status:** Added Web Push (Push API + service worker) and an in-app notification feed on top of Phase 17's email, per `PHASE_18.md`/`prompts/phase-18.md` — user explicitly asked for notifications "when the app is not opened," which email alone doesn't satisfy (only push does, since it wakes the service worker even with the tab/PWA fully closed). Mobile push (FCM/APNs) was ruled out — no native app. **`notificationService.dispatch` now fans out to three independent channels** instead of just email: `dispatchEmail` (Phase 17's flow, unchanged), `dispatchPush` (new), `dispatchFeed` (new) — run via `Promise.all`, each wrapped in its own try/catch so one channel's failure (no push subscriptions, a rejected web-push send, a feed-insert error) can never affect the others or propagate back into the `tasksService` method that triggered the call. **`notification_log` extended with a `channel` column** (`'EMAIL' | 'PUSH'`, default `EMAIL`), and its unique constraint widened to `(task_id, type, dedup_key, channel)` — `notificationsRepository.claim()` now takes an explicit `channel`, so a push claim and an email claim for the same event are tracked as fully independent slots, each with its own dedup/retry lifecycle. **New tables:** `push_subscriptions` (one row per device, `unique(endpoint)` so re-subscribing the same browser upserts rather than duplicates) and `notifications` (the in-app feed — one row per event, always inserted regardless of email/push outcome, no dedup/retry semantics since it isn't a delivery attempt). **New services/repos:** `services/pushService.js` (thin `web-push` wrapper — `sendPush(subscription, {title, body, url})` never throws, returns `{ok, error?}`, and self-prunes the subscription via `pushSubscriptionsRepository.removeById` on a 404/410 send response), `services/pushSubscriptionsService.js`, `services/notificationsFeedService.js`, `repositories/pushSubscriptionsRepository.js`, `repositories/notificationsFeedRepository.js`. `notificationTemplates.js` gained `buildPush(type, {task, ...extra}) -> {title, body, url}`, a shorter form shared by both push and the feed (email keeps its own fuller HTML `buildEmail`). **`attemptSendPush` sends to every one of a user's subscriptions under one claimed row per event** (not one row per device) — the row tracks "has this event's push been attempted," and is marked `SENT` if any device accepted it, `FAILED` only if every device rejected. `retryFailed()` is now channel-aware: a `PUSH` row re-fetches the user's current subscription list and retries against all of them; an `EMAIL` row behaves exactly as in Phase 17. **New endpoints:** `POST/DELETE /api/push/subscribe`, `GET /api/push/vapid-public-key` (deliberately not behind auth — the key itself isn't sensitive and the client needs it before it has a session to authenticate a push flow with), `GET /api/notifications` (`?unreadOnly=true`), `POST /api/notifications/:id/read`, `POST /api/notifications/read-all` — all ownership-scoped via the same `getOrCreateUser`-then-scope pattern every other controller uses. **Frontend:** hand-written `client/public/sw.js` (no `vite-plugin-pwa`, matching the project's preference for hand-built code over generator-owned config elsewhere) handling `push`/`notificationclick`; `lib/pushClient.js` (permission request, service-worker registration, subscribe/unsubscribe, `isPushSupported`/`getPushPermissionState`/`isSubscribed` state checks); `lib/notificationsStore.jsx` (`NotificationsProvider`, mirroring `tasksStore.jsx`'s shape — holds `unreadCount`/`sheetOpen`, deliberately does **not** fetch on mount since it wraps the whole app including signed-out routes, so `AppLayout` triggers the first `refreshUnreadCount()` itself, the one place guaranteed to only render for a signed-in user); `hooks/useNotificationsFeed.js` (refetches the full list on sheet-open only, no polling); `components/notifications/NotificationBell.jsx` (AppBar's previously-unused trailing-action slot, now wired on every authenticated screen, amber badge dot matching `BottomNav`'s existing needs-review dot styling) + `NotificationsSheet.jsx` (reuses the Phase 12 `Sheet` primitive, mark-one-read on row click/mark-all-read button, routes each row to its task via a best-effort `type -> status` map). **Opt-in placement decision, confirmed with the user before implementation** (flagged as open in `PHASE_18.md` §5): Profile toggle (`NotificationsSettingsRow`, replacing the old static "Notifications: On" row) as the durable control, paired with a one-time dismissible Home banner (`NotificationsBanner`, shown once per device via a `localStorage` flag, never shown if push is unsupported or the OS permission is already resolved one way or another). **New env vars** (`VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`) added to `example.env`; `web-push` added as a real server dependency. A fresh VAPID keypair was generated this session (`npx web-push generate-vapid-keys`) and written directly to the user's real (gitignored) `server/.env` — not committed anywhere. **Testing:** `tests/push.service.test.js` (mocked `web-push` — success, never-throws + auto-prune on 404/410, no prune on other failures), `tests/notifications.templates.test.js` extended for `buildPush`, `tests/notifications.service.test.js` heavily extended (push fan-out with 0/1/many subscriptions, per-channel independent claim/dedup, a rejected push claim never blocking the email send, the feed always writing regardless of email/push outcome, channel-aware `retryFailed`), `tests/push.subscriptions.test.js` and `tests/notifications.feed.test.js` (new integration files — auth requirement, upsert-on-conflict, cross-user isolation, ownership-scoped mark-read returning 404 for a foreign id, unread filtering), `tests/notifications.integration.test.js` extended with a dedicated "push failures never fail task operations" block. Frontend: two new test files (`notificationsFeed.test.jsx`, `pushOptIn.test.jsx`, 17 tests) plus the full existing suite — **104/104 frontend tests passing**, `npm run build` succeeds, `npx oxlint src` shows only the same pre-existing warning shapes (the new `notificationsStore.jsx` `only-export-components` warning is the identical shape already accepted for `tasksStore.jsx`/`toastStore.jsx`). **Migration `20260819150000_add_push_and_feed.sql` applied by the user through the Supabase SQL editor** — backend suite re-run afterward: **137/137 passing**, including every push-subscription, in-app-feed, and channel-aware-dedup integration test that had failed pre-migration. VAPID keys generated this session (`npx web-push generate-vapid-keys`) and written to the user's real (gitignored) `server/.env`; the user then changed `VAPID_SUBJECT` to their own real address (`mailto:tarunvalluri3@gmail.com`) — read live from the env at request time, no code change needed. **Still not verified live** (needs a real browser, which this environment can't drive for a real OS permission prompt/service-worker push round trip): actually granting notification permission, receiving a real push while the tab is closed, and the iOS-Safari-requires-PWA-install constraint documented in `PHASE_18.md` §0.

**Phase:** 17 — Notifications & Email. Done.
**Overall status:** Built server-triggered transactional email via Resend on top of the finished 0–16 roadmap, per the full design in `PHASE_17.md`/`prompts/phase-17.md`. **New layer, kept out of controllers and out of `tasksService`'s core lifecycle rules:** `services/emailService.js` (thin Resend wrapper — one `sendEmail({to, subject, html, text})`, throws on failure, no other opinion), `services/notificationTemplates.js` (pure `buildEmail(type, {task, ...extra}) -> {subject, html, text}`, no I/O), `services/notificationService.js` (orchestration: claim-then-send-then-mark against `notification_log`, the actual mechanism behind "email failures never affect task operations" — every `notifyX` function catches everything internally and never throws), `repositories/notificationsRepository.js` (Supabase calls only), `services/clerkService.js` (fetches a Clerk user's primary email via `clerkClient.users.getUser`, returns `null` rather than throwing on any failure). **New `usersService.js`** (`getOrCreateUser`) replaces the old direct `usersRepository.findOrCreateByClerkUserId` call in `tasksController` — it composes the Supabase-only repository lookup with the Clerk email fetch, keeping `usersRepository` itself Supabase-calls-only per `CLAUDE.md`'s layering rule; `usersRepository` was split into `findByClerkUserId`/`findById`/`create`/`updateEmail`. **Seven notification types**, six wired as immediate side effects of the matching `tasksService` method (`createTask`→`TASK_CREATED`, `completeTask`/`resolveMissedTask(COMPLETED)`→`TASK_COMPLETED`, `resolveMissedTask(INCOMPLETE)`→`TASK_INCOMPLETE`, `deleteTask`→`TASK_DELETED`, `editTask`→`TASK_UPDATED` only when `deadline` or `priority` actually changed value, compared by parsed timestamp for `deadline` since the Zod-coerced `Date` and the DB's ISO string are never `===`-equal even when unchanged) — each call `await`ed so a restart-vulnerable serverless deploy would still complete the send before responding, but wrapped internally so a Resend failure can never turn into a failed task-operation response. **`TASK_MISSED` and `DEADLINE_REMINDER` fire only from a new scheduled sweep**, deliberately decoupled from the existing lazy per-request `ACTIVE→MISSED` check in `tasksService.maybeTransitionToMissed` — that check still only flips status on read, exactly as before, and now never triggers an email itself (verified live by a dedicated test asserting zero `notification_log` rows and zero `sendEmail` calls immediately after a lazy transition). **`jobs/notificationScheduler.js`** runs `runNotificationSweep()` via a plain `setInterval` (`NOTIFICATION_SWEEP_INTERVAL_MINUTES`, default 5, plus one run 10s after boot), started once from `src/index.js` after `app.listen` — never during tests, since the suite imports `app.js` directly and never `index.js`. Each tick: (1) a new cross-user `tasksRepository.sweepAllUsersMissed` (no `user_id` filter — the one place this phase intentionally breaks from every other query's per-user scoping, safe only because it's server-internal and never exposed via a route) flips every user's overdue `ACTIVE` tasks to `MISSED` and fires `TASK_MISSED` for each; (2) a new `tasksRepository.findActiveDueWithin` finds `ACTIVE` tasks due inside `DEADLINE_REMINDER_HOURS_BEFORE` (default 24) and fires `DEADLINE_REMINDER`; (3) `notificationService.retryFailed()` re-attempts any `FAILED` row under a 5-attempt cap (`notification_log.attempts`), refetching the user/task fresh rather than caching stale content. **Restart-safety and dedup both come from the database, not memory:** `notification_log` (migration `20260819140000_add_notifications.sql`, applied by hand through the Supabase SQL Editor — same manual process as every prior migration) has a `unique (task_id, type, dedup_key)` constraint that `notificationsRepository.claim()` relies on directly — inserting a `PENDING` row first and treating a `23505` violation as "already claimed, skip" is the actual duplicate-prevention mechanism, not an in-process check. `dedup_key` is `'once'` for the five naturally-single-fire types, the task's own `updated_at` for `TASK_UPDATED` (so repeat edits each get their own email but retries of the same edit dedupe), and the reminder-window label (e.g. `'24h_before'`) for `DEADLINE_REMINDER`. `users` gained a nullable `email` column, synced by `usersService.getOrCreateUser` on first creation and lazily backfilled on read for any pre-existing row. **`TASK_INCOMPLETE` inclusion was confirmed with the user before implementation** (flagged as an open judgment call in `PHASE_17.md`, since it wasn't in the original literal example list) — approved as the terminal negative-verdict counterpart to `TASK_COMPLETED`. **New env vars** (`RESEND_API_KEY`, `RESEND_FROM_EMAIL`, `CLIENT_URL`, `NOTIFICATION_SWEEP_INTERVAL_MINUTES`, `DEADLINE_REMINDER_HOURS_BEFORE`) added to `example.env`, no real secrets committed; `resend` added as a real dependency. **Testing:** `tests/notifications.templates.test.js` (pure template unit tests per type, including that `TASK_MISSED`'s copy never implies "never completed" — kept consistent with `CLAUDE.md`'s MISSED-is-a-checkpoint rule), `tests/notifications.service.test.js` (`notificationService`/`emailService`/repositories fully mocked — dedup on a repeated claim, never-throws on a missing user/missing email/rejected send/rejected DB call, `TASK_UPDATED`'s per-edit dedup key, `retryFailed`'s resend-and-mark-SENT and cap-out-when-unretryable paths), `tests/notifications.integration.test.js` (real DB, `emailService` mocked at its own boundary per the phase doc's plan — proves `POST`/`complete`/`DELETE`/`PATCH`/`resolve-missed` all still return their correct 2xx even when the mocked Resend call is forced to reject, confirms a title-only edit sends nothing while a deadline edit does, confirms the lazy MISSED check sends nothing), `tests/notifications.scheduler.test.js` (calls `runNotificationSweep` directly, not through the real timer — overdue task flips to `MISSED` and gets exactly one `TASK_MISSED` log row across two repeated sweep calls, a not-yet-due task is left untouched, a due-soon task gets exactly one deduped `DEADLINE_REMINDER`, a far-out task gets none). **Full suite: 103/103 passing** (was 73 at the close of Phase 8, plus Phase 16's frontend suite separately). **One real, disclosed test-isolation gap surfaced during this phase, not fully solved:** `runNotificationSweep` is genuinely cross-user by design, and this project's test suite still has no dedicated test Supabase project (an unresolved constraint flagged since Phase 7) — so a sweep triggered by `notifications.scheduler.test.js` can, in principle, also transition an overdue task belonging to another concurrently-running test file's user. This was caught immediately: an early version of the flaky test asserted a raw `sendEmail` call count and failed intermittently (2 calls instead of 1) exactly because of this; fixed by rewriting the assertion to be scoped to the test's own `task_id` via a direct `notification_log` query instead of counting global mock calls, which is immune to the cross-file interference and actually verifies the invariant that matters (exactly one `TASK_MISSED` per task, deduped). The underlying cross-user-sweep-vs-shared-test-DB tension itself remains — a dedicated test project would remove it entirely, same conclusion as the Phase 7 note. **Live-verified after the user added real `RESEND_API_KEY`/`RESEND_FROM_EMAIL`/`CLIENT_URL` to `server/.env`:** a real end-to-end send was confirmed via a temporary one-off script calling `emailService.sendEmail` directly (not part of the test suite — written, run once, then deleted, matching this project's established ad-hoc-live-verification pattern) — Resend accepted the send and returned a real message id (`b8bab9f6-ae35-44b3-867f-39f1ac5ad659`) for an email sent to a real inbox at the user's request. **Still not verified live:** the scheduler's `setInterval` mechanism over a real elapsed multi-tick window in a running deployment (only the underlying `runNotificationSweep` function was exercised directly, per the phase's own explicit testing plan) — its 10-second-after-boot and interval-timer wiring in `src/index.js` is straightforward enough that this wasn't considered a live-verification blocker, but it's real production behavior that hasn't been watched happen; and the full task-lifecycle-triggered emails (`TASK_CREATED` etc.) going out from the real running app against a real Clerk session (no real Clerk credentials available non-interactively, consistent with every prior phase) rather than the mocked-Clerk test suite or this direct `emailService` smoke test.

**Phase:** 16 — Frontend Testing & Polish. Done. **This closes the initial 0–16 roadmap.**
**Overall status:** Set up a real frontend test runner and used it as the base for a full audit-and-polish pass over everything Phases 9–15 shipped — no new features, per the phase's explicit scope. **Test infra:** `client/vitest.config.js` (jsdom environment, `src/test/setup.js` as `setupFiles`, mirroring `server/vitest.config.js`'s shape for consistency) + `@testing-library/react`/`jest-dom`/`user-event` as new devDependencies; `npm test` (`vitest run`) / `npm run test:watch` added to `client/package.json`. `src/test/setup.js` shims three things jsdom doesn't implement that components actually touch at render time: `matchMedia` (Framer Motion's `useReducedMotion`), `ResizeObserver`/`IntersectionObserver`, and a patched `getBoundingClientRect` (Recharts' `ResponsiveContainer` renders nothing at a 0×0 measured size, which jsdom always reports). `src/test/testUtils.jsx` has `renderWithProviders` (Router + `TasksProvider` + `ToastProvider`, the same stack `App.jsx` uses minus `ClerkProvider`, which is mocked per-file instead since it needs a real publishable key) and `buildTask()`, a factory for a schema-valid task object per `CLAUDE.md`'s data model. **87 tests across 16 files, all passing.** Component tests: `StatusChip`/`PriorityRail`+`PriorityLabel` (chip/rail wording and color-token correctness), `ReasonField`/`DeleteSheet`/`ResolveSheet` (validation, required-reason gating, verbatim §3.3 copy), `TaskForm` (client validation, create vs. edit, server-validation-detail-to-field mapping), `TaskCard`, `BottomNav`, dashboard (`StatTile`/`StatusCounts`/`DueSoon`/`RecentActivity`), analytics (`ChartCard`/`CompletionTrendChart`/`ReasonBreakdownChart`/`PriorityBreakdown`/`UnresolvedMissed`) — rendering/props correctness per the phase's explicit "not pixel-perfect visuals" scope. **`lifecycleAffordances.test.jsx` asserts DESIGN.md §7.3 directly** on both `TaskCard` and `TaskDetail`: Complete/Edit/Delete render only on ACTIVE, Resolve only on MISSED, nothing renders on COMPLETED/INCOMPLETE/DELETED (parametrized over all three terminal statuses), plus a dedicated check that an INCOMPLETE task which passed through MISSED shows both `missed_reason` and `incomplete_reason` as distinct, both-preserved text. **Integration tests** (`src/test/integration/`) against a fully mocked `tasksApi`: create (via the real `BottomNav` → Create sheet → `TaskForm` path), edit (via `TaskDetail` → Edit sheet), complete, delete, resolve MISSED→COMPLETED, resolve MISSED→INCOMPLETE (reason required), filter-by-status (`TaskList` requests the correct `?status=` for all five sections), dashboard (`Home` empty/loaded/error+Retry states, confirming the error path never leaks the raw `"boom"` message), and insights (`Insights` empty/loaded states, chart section titles present). **Auth-flow tests:** `ProtectedRoute.test.jsx` (skeleton while Clerk is still resolving, redirect to `/login` when signed out, renders the protected route when signed in) and `Root.test.jsx` (the one route that resolves two entirely different experiences by auth state outside the route-guard pattern — Landing when signed out, the real `Home` dashboard shell when signed in), both with `@clerk/clerk-react` mocked per-file. **Mobile viewport verification:** same established ad-hoc pattern as every prior phase — `playwright` installed with `--no-save`, `window.__TEMP_BYPASS_AUTH__` wired into `Root.jsx`/`ProtectedRoute.jsx` and fully reverted after (confirmed via a clean `git diff` on both files), full network-level mocking of `/api/tasks*` with a five-task fixture spanning every status, one with a long title and both `missed_reason`/`incomplete_reason` set. At a 390×844 viewport, walked Home, all five task-list sections, task detail (confirmed both reasons render), the Create sheet, and Insights: zero `document.documentElement.scrollWidth` overflow anywhere, zero console errors, and re-checked every route again with `html { font-size: 32px }` (the 200%-text-scaling proxy used since Phase 9) — still zero horizontal overflow on any of them. Also screenshotted the task list in full CSS `grayscale(1)` (§8's specified check): every status chip stayed legible by its word (Active/Needs review/Completed/Not done/Deleted) and priority still read via the rail's brightness step, confirming color is never the only signal. **Real bug found and fixed by this pass:** `FilterRow`'s scroll-snapping status pills measured ~32px tall (`px-3 py-1.5 text-meta` with no explicit height), well under the §8 "44×44 tap-target floor" acceptance criterion that applies everywhere — caught by an automated tap-target sweep during the mobile-viewport pass, not by inspection. Fixed by adding `min-h-(--size-tap-min)` to the pill (`client/src/components/nav/FilterRow.jsx`); re-verified at 390×844, pills now meet the floor without materially changing the compact-chip look. **§9 mobile-checklist walk-through, item by item:** viewport meta, safe-area insets, `100dvh`, theme-color, tap feel, 16px input floor, `overscroll-behavior-y: contain`, pull-to-refresh, optimistic updates, and UTC-in/local-out time formatting were all already in place from Phases 9–13 and re-confirmed still correct. **Two real, previously-unaddressed gaps found on this checklist and fixed:** (1) **Offline** — no phase had ever wired up an online/offline signal at all. Added `src/hooks/useOnlineStatus.js` (`online`/`offline` window events) and `src/components/ui/OfflineBar.jsx` (a dismissible bar above `BottomNav`, verbatim §9 copy — *"You're offline. Changes will fail until you reconnect."* — amber-toned via existing `--color-review` tokens, re-arms itself if dismissed then the connection drops again later), mounted in `AppLayout`. Existing lifecycle actions already satisfy the checklist's "actions disable rather than silently failing" in spirit without further change: `useTaskLifecycle`'s error path (`lib/taskLifecycle.js`) already maps any non-`ApiError` failure (which is what an offline `fetch` throws) to the same generic *"Something went wrong. Try again."* toast plus a visual rollback, rather than a silent failure — a true tap-by-tap button-disable-while-offline pass was judged more than this polish phase's scope justifies and was not added. (2) **Installable** — no web app manifest existed anywhere in `client/`. Added `client/public/manifest.webmanifest` (`display: standalone`, `#0D0D0D` background/theme color, `favicon.svg` as an `any`+`maskable` SVG icon) and linked it from `index.html`. **Known limitation, not fixed:** the manifest's icon is the existing SVG only — true maskable-safe raster PNG icons were never generated (flagged as an open gap all the way back in the "Brand mark re-imported" decisions-log entry pre-Phase-9) and still aren't; SVG-only manifest icons work in current Chromium/Android but aren't universally supported, so this is a partial fix of the checklist item, not a complete one. **DESIGN.md §2.6 contrast audit:** every hex value in `tokens.css` was diffed line-by-line against the §2.6 table — all match exactly (colors unchanged since Phase 9), so every previously-verified contrast ratio still holds; no drift found. **Hex-value grep:** `grep -rnE "#[0-9a-fA-F]{3,8}" src/components` returns zero matches — every component still reads color exclusively through `var(--color-*)` tokens. **§3.3 copy audit:** grepped for "fail"/"failed"/"invalid input"/"error occurred" across `src/components`, `src/routes`, `src/layouts` — zero matches outside test fixtures; every error surface (`ErrorState`, `TaskForm`'s server-error line, `lib/taskLifecycle.js`) uses the mapped, non-accusatory copy and never a raw API message. **`npx impeccable detect src`** returns only the same pre-existing, already-approved `overused-font` advisory on Geist (settled in `DESIGN.md` §11 since before Phase 9) — no new findings from any Phase 16 file, confirmed both before and after every fix in this phase. **Other cleanup:** removed one genuinely dead `DAY` constant from `lib/datetime.js` (an `oxlint` `no-unused-vars` warning present since an earlier phase); `npx oxlint src` now shows the same 5 pre-existing advisory warnings it showed at the close of Phase 15 (all `only-export-components`/`exhaustive-deps` shape, none new). **Performance review:** checked `useDashboardTasks`/`useTaskList`/`useTaskCounts` for redundant fetching — each already re-fetches only on its own `version` bump from `TasksProvider`, no polling, no redundant effects found; checked every `motion`/`AnimatePresence` usage app-wide for the §5 "transform and opacity only, never height/top/left/width" rule — `PullToRefresh` (fixed in Phase 12), `TaskCard`'s flourish, `Sheet`, `Toast`, and every chart's (lack of) entry animation all confirmed transform/opacity-only; no violations found, nothing to fix. **Accessibility review against §8:** confirmed semantic HTML (`<article>` cards, real `<label>`s on every form field, `<dl>`/`<dt>`/`<dd>` in `TaskDetail`), focus rings (`:focus-visible` in `globals.css`), sheet focus traps + focus return (`Sheet.jsx`'s Tab-cycle + `returnFocusRef`, already covered by the `DeleteSheet`/`ResolveSheet` Escape tests in this phase's suite), `aria-live="polite"` on the toast viewport, and card accessible names (`TaskCard`'s `aria-label` format, asserted directly in `TaskCard.test.jsx`) — all already correct from Phases 11–14, re-verified rather than changed. Reduced motion verified via the existing `prefers-reduced-motion: reduce` media query in `globals.css` plus the Playwright `reducedMotion: 'reduce'` context passes from Phases 12–15 (not re-run this phase; the mechanism is unchanged). **Flagged, not fixed (per the phase prompt's "don't force it, note it" allowance):** `DESIGN.md` §8's illustrative toast-announcement string (*"Completed. Ship the Q3 pricing memo moved to Completed."*) is fuller than the actual shipped toast copy (`"Completed"`/`"Deleted"`/`"Confirmed not done"`), which instead matches §3.3's own literal rule ("the toast confirms in past tense: *Completed* · *Deleted* · *Saved*"). The two sections read as in tension; since §3.3 is the dedicated Voice section and its wording is exact and prescriptive while §8's string reads as illustrative rather than a copy mandate, this phase treated the shipped short-form copy as compliant and left it unchanged rather than guessing which section should yield.

**Phase:** 15 — Analytics. Done.
**Overall status:** Built the real Insights view on top of Phase 11's locked placeholder — trend, reason, and priority analysis over the same historical task data the rest of the app already preserves. **Two scope decisions confirmed with the user before starting (both logged below):** aggregation stays client-side (no new backend endpoint), and charts render via **Recharts** rather than hand-rolled SVG. **Data layer:** `lib/analyticsStats.js` — pure functions over the one unfiltered task array `useDashboardTasks` (Phase 14) already fetches, reused as-is here rather than duplicating a fetch hook: `computeCompletionTrend` (weekly buckets, split into on-time vs. resolved-from-missed), `computeIncompleteTrend`/`computeDeletedTrend` (weekly buckets keyed to `incomplete_at`/`deleted_at`), `computeIncompleteReasonBreakdown`/`computeDeletedReasonBreakdown` (free-text reasons ranked by exact-trimmed-match frequency, top 5 + "Other"), `computeUnresolvedMissed` (oldest-first list, not a trend — see below), and `computePriorityBreakdown` (incomplete-rate per priority, denominator = COMPLETED + INCOMPLETE only, since ACTIVE/MISSED/DELETED tasks haven't reached a completed-vs-incomplete verdict). Weekly bucketing is hand-rolled UTC Monday-week math (no date library, consistent with `lib/datetime.js`'s existing house rule) rather than a library. **Design deliverable, written back into `DESIGN.md` §7.4** per the phase's explicit requirement: series palette, axes/grid, label typography, empty/loading states, and the reduced-motion decision. **Key palette call:** the app's source palette (§2.1) is deliberately low-chroma/muted — validated against the `dataviz` skill's categorical-palette checker, which correctly flagged it as failing the chroma-floor and lightness-band checks built for a standard vivid categorical scheme. Rather than introduce new, more-saturated colors that would fight the rest of the approved system, every chart in this phase reuses **existing status/priority tokens directly**: completion/incomplete/deleted trends stay green/Foggy/Mid-Grey (the same hues already on every `StatusChip`); the two completion paths (on-time vs. resolved-from-missed) share one hue at two opacities (`--color-completed` solid vs. a new `--color-chart-completed-resolved` token, a `color-mix()` of it) rather than a second categorical color, since both are semantically "completed"; the priority breakdown reuses `PriorityRail`'s exact monochrome rail colors instead of inventing chart-only hues (only 3 categories, so every bar is direct-labeled, no legend needed); ranked reason bars use one flat status-matched hue since bar length already encodes magnitude. Red never appears in any chart. Two more new tokens in `tokens.css`: `--color-chart-grid` (= `--border-hairline`) and `--color-chart-axis-label` (= `--color-ink-3`). **Components:** `components/analytics/` — `ChartCard` (title + description wrapper), `ChartStates.jsx` (`ChartSkeleton`/`ChartEmpty`, fixed-size shimmer/empty variants matching the rest of the app's loading convention), `chartTheme.js` (shared axis/tooltip/legend style constants so no chart file repeats inline style objects), `CompletionTrendChart`, `IncompleteTrendChart`, `DeletedTrendChart` (stacked/simple weekly bar charts), `ReasonBreakdownChart` (shared component parametrized by title/data/color, used for both incomplete and deleted reasons — ranked horizontal bars, reason labels in Sans since they're the user's own free-typed text per §2.2's "warm" rule, counts in Mono), `PriorityBreakdown` (monochrome horizontal bars, direct percentage labels), `UnresolvedMissed` (not a chart — a live amber-bordered count + oldest-first list, since `MISSED` is CLAUDE.md's pending checkpoint, never a terminal outcome to trend over time). **`Insights.jsx`** implements all four `DESIGN.md` §7.1 states (loading: three sized chart skeletons; error: mapped copy + Retry, never the raw API message; empty: "Nothing to show yet."; loaded: `UnresolvedMissed` + all six charts stacked full-width). Bottom-nav context line for `/insights` updated from `"LOCKED · PHASE 15"` to `"PATTERNS"` in `AppLayout.jsx` — no other nav changes, per the phase's explicit "no navigation changes needed" note. **Chart entry has no animation** in this phase (Recharts' default mount transition disabled) — logged in `DESIGN.md` §7.4 as a deliberate scope call rather than a `§5.6` reduced-motion gap: data is simply present on first paint, which reads better for a "read your own history" view than a delight moment, and sidesteps the need for a reduced-motion branch entirely. **Verified live:** `npm run build` succeeds (Recharts pushes the main bundle over Vite's 500KB chunk-size warning threshold — a size warning only, not a build error, and an expected/accepted cost of the user's explicit choice to use a charting library); `npx oxlint src` shows no new findings (same pre-existing warnings as Phase 14, unchanged shape); `npx impeccable detect src` returns only the pre-existing, already-approved `overused-font` advisory, nothing new from any Phase 15 file. A temporary Playwright pass (`playwright` installed with `--no-save` and fully removed after; `window.__TEMP_BYPASS_AUTH__` wired into `Root.jsx`/`ProtectedRoute.jsx` and fully reverted after, confirmed via a clean `git diff` on both files post-revert — no real Clerk credentials available non-interactively, same constraint as every prior phase) against the dev server with full network-level mocking of `/api/tasks*`, using a realistic dataset spanning on-time completions, resolved-from-missed completions, confirmed-incomplete tasks with repeated reasons (to exercise the ranking/"Other" logic), deleted tasks with reasons, unresolved MISSED tasks, and mixed priorities, confirmed: every chart renders with correct data, colors, and direct labels at a 390×844 mobile viewport; zero console errors across loaded/empty/error/reduced-motion passes; the error state shows the mapped copy and never leaks the raw `"boom"` API message; the empty state shows the verbatim copy; no horizontal page-body scroll (`document.documentElement.scrollWidth` measured, not just eyeballed). **One real debugging note, not a shipped bug:** an initial `page.screenshot({ fullPage: true })` capture showed several charts with no visible bars; root-caused to Playwright's full-page capture forcing a viewport/layout resize that raced Recharts' `ResponsiveContainer` resize-observer repaint, not an app bug — confirmed by re-screenshotting the same page at a fixed tall viewport (no `fullPage` flag), which showed every bar rendering correctly with real SVG geometry and correctly `var()`-resolved fill colors (checked via `getComputedStyle`). Also fixed one real, still-shipped bug found during this same visual pass: the reason-breakdown Y-axis category labels were double-line-wrapping mid-word (Recharts' own tick-wrapping kicking in before the component's 20-character truncation had a chance to shorten the string enough to fit the 128px axis width) — fixed by tightening the truncation to 14 characters and the axis width to 112px with `interval={0}`, re-verified clean single-line truncated labels afterward. **Not verified live:** an actual signed-in session hitting the real backend end-to-end with real historical task data (no real Clerk credentials non-interactively, consistent with every prior phase) and real-device 200% OS text-scaling behavior on the new chart labels (same unchanged `rem`-based mechanism as every prior phase, not re-verified on a physical device).

**Phase:** 14 — Dashboard. Done.
**Overall status:** Built the real Home dashboard on top of Phase 11's placeholder, giving a quick overview of the task landscape from data the app already fetches. **Data layer:** `hooks/useDashboardTasks.js` — one unfiltered `GET /api/tasks` (via the existing `tasksApi.list()`), surfaced with its own real `loading`/`error`/`loaded` states (unlike `useTaskCounts`, which deliberately swallows fetch failures since its counts are decorative chrome elsewhere) so the dashboard can show a genuine error state per §7.1. `lib/taskStats.js` is a pure client-side aggregation module over that one array — `computeStatusCounts` (tally by status), `computeRecentActivity` (one entry per task, keyed to whichever timestamp matches its *current* status — `completed_at`/`missed_at`/`incomplete_at`/`deleted_at`/`created_at` for ACTIVE — sorted most-recent-first, capped at 5), and `computeDueSoon` (ACTIVE tasks with a deadline still ahead but within 24h, capped at 3). No new backend aggregation endpoint, per the phase's explicit scope — all three are pure functions over the already-fetched list. **Components:** `components/dashboard/StatTile.jsx` (mono `display`-scale numeral + micro-scale colored label), `StatusCounts.jsx` (2-up grid of five `StatTile`s reusing `StatusChip`'s `statusLabel` so wording can't drift — "Needs review"/"Not done" per §2.4 — with the fifth tile, Deleted, spanning both columns since five doesn't divide evenly), `DueSoon.jsx` (the phase's "highlight anything time-sensitive" requirement — an amber-bordered heads-up list, explicitly never red, rendering nothing when empty rather than an empty section header), `RecentActivity.jsx` (a plain list, title + verb + relative/absolute timestamp via the existing `formatDeadline` — reused as-is since it already handles arbitrary past/future ISO timestamps, not just deadlines). `Skeleton.jsx` grew `DashboardStatSkeleton` (five shimmer blocks in the same 2-up + full-width-fifth shape as the real grid) for the explicit "skeleton for the stat grid" acceptance criterion. **Four states:** `Home.jsx` now implements loading (stat-grid skeleton), empty (no tasks created yet at all — "Nothing here yet." + a New task button opening the shared create sheet), error (verbatim `ErrorState` copy + Retry, never a raw API message), and loaded (greeting + `StatusCounts` + `DueSoon` + `RecentActivity`). No hex values in any new file — every color is a `var(--color-*)` token, several reused directly from `StatusChip`/`DeadlineDisplay` rather than redefined. **Verified live:** `npm run build` succeeds; `npx oxlint src` shows no new findings (same pre-existing warnings as Phase 13, unchanged shape); `npx impeccable detect src` returns only the pre-existing, already-approved `overused-font` advisory on Geist, nothing new from any Phase 14 file. A temporary Playwright pass (17 assertions, `playwright` installed with `--no-save` and fully removed after, `window.__TEMP_BYPASS_AUTH__` wired into `Root.jsx`/`ProtectedRoute.jsx` and fully reverted after — no real Clerk credentials available non-interactively, same constraint as every prior phase) against the dev server with full network-level mocking of `/api/tasks*` confirmed: the loaded state shows correct per-status counts, correct chip wording, a Due Soon entry for a near-deadline ACTIVE task while excluding a far-out one, and a Recent Activity list correctly ordered by each task's own event timestamp; the empty state (zero tasks) shows the verbatim copy and a working New task button; a forced 500 shows the mapped error copy with a working Retry and never leaks the raw `"boom"` API message; and the loaded state still renders correctly under `reducedMotion: 'reduce'`. Also screenshotted the loaded dashboard at a 390×844 mobile viewport and visually confirmed the 2-up stat grid, mono tabular numerals, correct status colors, the amber (never red) Due Soon border, and the Recent Activity list all render as intended. **Not verified live:** an actual signed-in session hitting the real backend end-to-end with real task data (no real Clerk credentials non-interactively, consistent with every prior phase) and real-device 200% OS text-scaling behavior (same unchanged `rem`-based mechanism as every prior phase, not re-verified on a physical device).

**Phase:** 13 — Task Lifecycle UI. Done.
**Overall status:** Built Complete/Delete/Resolve actions on top of Phase 12's read/create/edit layer, wired to `POST /api/tasks/:id/complete`, `DELETE /api/tasks/:id`, and `POST /api/tasks/:id/resolve-missed`. **New:** `apiClient.js` grew `tasksApi.complete`/`resolveMissed`/`remove`; `ReasonField.jsx` (textarea, trims before validating, live count past 120, shared helper copy per §3.3 "kept as history..."); `DeleteSheet.jsx` (reason required, the destructive-red confirm button — the only red button in the app, per §2.4); `ResolveSheet.jsx` (the two outcomes at equal visual weight, neither pre-selected, verbatim §3.3 copy — "The deadline passed before this was confirmed. What actually happened?", "I completed this" / "I didn't complete this", "What got in the way?"); `CompleteAction.jsx` (a lightweight Sheet-based confirm, since Complete is low-risk); `Toast.jsx` + a new `lib/toastStore.jsx` (one-at-a-time queue, `aria-live="polite"`, 3.2s, positioned above `BottomNav`, mounted once in `AppLayout`); `lib/taskLifecycle.js` (maps `ApiError` status codes to the §3.3 error voice — 409 → *"This task was already resolved somewhere else. Pull to refresh."*, generic fallback *"Something went wrong. Try again."*, never a raw API message). **The shared engine:** `hooks/useTaskLifecycle.js` — one hook behind Complete/Delete/Resolve on both `TaskCard` and `TaskDetail`. It runs the API call and a minimum animation-duration timer concurrently (`Promise` for each, `await`ed together) so a fast network response never truncates the flourish and a slow one never leaves the UI dangling mid-animation; on success it calls `onSettle(updatedTask)` + bumps `refreshTasks()` + shows the past-tense toast, on rejection it calls `onRollback()` + shows the mapped error toast. `TaskCard` uses `onSettle` to actually remove the card from `TaskList`'s local array (a new `removeLocal` on `useTaskList`, filtering the fetched array without waiting for a `version`-triggered refetch) and `onRollback` to reset its own visual stage — there's deliberately no matching "restore" path, since a rejected action never removed the card from the list to begin with, only its own in-place animation state. **The completion flourish (§5.4):** implemented in `TaskCard` as an explicit `stage` state machine (`idle → press → wash → check → exit`) driven by `setTimeout`s at the spec's own millisecond marks (0/90/200/380), rendering a `scaleX` green wash pseudo-layer and an SVG `pathLength`-drawn checkmark over the card, then an opacity+scale exit; delete and resolve-as-INCOMPLETE skip straight to a plain 200ms opacity fade (`stage: 'fade'`), matching "resolving as COMPLETED plays the completion sequence, resolving as INCOMPLETE gets the sober treatment." Reduced motion collapses everything to a single 120ms opacity fade, verified live. **Action placement:** both `TaskCard` (compact row) and `TaskDetail` (full-width row) render Complete/Edit/Delete on ACTIVE and Resolve-only on MISSED, nothing on COMPLETED/INCOMPLETE/DELETED — `TaskDetail` reuses the identical `useTaskLifecycle` hook but doesn't need the flourish choreography (no list to close a gap in), so its `onSettle` just swaps in the server's returned task via the existing `onTaskUpdated` callback from Phase 12's edit flow. **Decision, logged:** `useTaskLifecycle`'s animation-duration timer runs even on the detail page (not just the list), so completing/resolving from `TaskDetail` also gets the same ~620ms/200ms pause before the button spinner clears — a deliberate simplification (one hook, one timing model everywhere) rather than a bug; the pause reads as consistent with the card's own pacing rather than as latency. **Verified live:** `npm run build` succeeds; `npx oxlint src` shows no new findings (same pre-existing `no-unused-vars`/`exhaustive-deps`/`only-export-components` advisories as before — the new `toastStore.jsx` warning is the same shape as the already-accepted `tasksStore.jsx` one); `npx impeccable detect` on `client/src` returns only the pre-existing, already-approved `overused-font` advisory on Geist, no new findings from any Phase 13 file. A temporary Playwright pass (18 assertions, `playwright` installed with `--no-save` and removed after, `window.__TEMP_BYPASS_AUTH__` wired into `Root.jsx`/`ProtectedRoute.jsx` and fully reverted after — no real Clerk credentials available non-interactively, same constraint as every prior phase) against the dev server with full network-level mocking of `/api/tasks*` (route-mocked, not the real Supabase-backed API, consistent with Phase 12) confirmed: Complete removes the card after the full flourish and shows a "Completed" toast; Delete's confirm button is disabled with an empty/whitespace reason and enables once one is entered, and removes the card after a plain fade; Resolve's two options render unselected with neither pre-checked, the INCOMPLETE branch requires a reason before Confirm enables while COMPLETED needs none, and each transitions/removes the card with the correct flourish-vs-fade treatment and toast ("Confirmed not done" / "Completed"); a forced 409 rejection on Complete shows the mapped *"...already resolved somewhere else..."* toast and the card visibly rolls back to fully opaque/present rather than staying removed; the entire Complete flow still correctly removes the card under `reducedMotion: 'reduce'`; Escape closes an open DeleteSheet and returns focus to its trigger button; and `TaskDetail` renders exactly the right action set per status (ACTIVE: Complete+Edit+Delete, MISSED: Resolve only, COMPLETED: none). Also screenshotted the Active list and confirmed Delete's trigger button carries no red — only the sheet's destructive confirm button would, per §2.4, not exercised in the static screenshot but confirmed by code review of `DeleteSheet`'s `variant="destructive"` placement. **Not verified live:** an actual signed-in session hitting the real backend end-to-end with real task data (no real Clerk credentials non-interactively, consistent with every prior phase) and real-device 200% OS text-scaling behavior on the new sheets/toast (same unchanged `rem`-based mechanism as every prior phase, not re-verified on a physical device).

**Phase:** 12 — Task Management UI. Done.
**Overall status:** Built the real task management layer on top of Phase 11's shell placeholders — creating, listing, viewing, and editing tasks, with all four `DESIGN.md` §7.1 states everywhere a list or detail view appears. **21st.dev MCP note:** the server was still not connected in this session (same gap flagged pre-Phase-9), so every component here was hand-built directly against `tokens.css` rather than generated then retokenized — no 21st.dev structure was sourced, and nothing needed the "stop and raise it" escape hatch since every surface expressed cleanly in the existing token set. **Data layer:** `tasksApi` added to `apiClient.js` (`list`/`get`/`create`/`patch`, mapping the route's lowercase status slug to the API's uppercase enum value); a small `TasksProvider` (`lib/tasksStore.jsx`) holds two bits of app-wide state without pulling in a state library — a `version` counter that any successful create/edit bumps (list/count hooks re-fetch on change, giving "reflects updates immediately after save" without prop-drilling a refetch callback everywhere) and `createSheetOpen`, so the Create sheet can be triggered both from the bottom nav's own button and from an Active-tab empty state's "New task" button. `useTaskList(status)` and `useTaskCounts()` (new `src/hooks/`) both read that `version`. Counts power `FilterRow`'s pill numbers and `BottomNav`'s needs-review badge — both stubbed at zero since Phase 11, now real (one unfiltered `GET /api/tasks`, tallied client-side, rather than five separate requests). **Components built:** `PriorityRail`/`PriorityLabel` (share a `priorityConfig.js` map so rail and label colors can't drift apart — monochrome only, per `DESIGN.md` §2.5, no hue), `StatusChip` (five variants, 14% `color-mix` tint, MISSED→"Needs review"/INCOMPLETE→"Not done" per §2.4), `DeadlineDisplay` (thin wrapper on Phase 9's `formatDeadline`), `TaskCard` (`<article>` with the §8 accessible-name format), `TaskList` (owns all four states + `PullToRefresh` + the §5.5 list-entry stagger), `TaskDetail` (full info incl. both `missed_reason` and `incomplete_reason` when an INCOMPLETE task has passed through MISSED first — reasons are never clamped/truncated here per §3.2), `TaskForm` (shared create/edit, native `datetime-local`, priority as a three-way segmented control, server validation errors mapped back onto fields via `ApiError.details`), and the real `Sheet` primitive (`components/ui/Sheet.jsx` — replaces Phase 11's inline `CreateSheet` stopgap in `BottomNav.jsx`: drag-handle-only drag-to-dismiss via Framer's `useDragControls`/`dragListener={false}` so dragging never fights scrolling or typing inside the sheet, a hand-rolled Tab-cycle focus trap, Escape-to-close, focus returns to the trigger via `returnFocusRef`). `EmptyState`/`ErrorState` extracted as generic primitives; `Skeleton.jsx` grew `TaskCardSkeleton`/`TaskCardSkeletonList`/`TaskDetailSkeleton`. **Routing:** task detail is a third URL shape, `/tasks/:status/:id`, added alongside (not replacing) the five literal list routes from Phase 11 — `AppLayout` now derives `isTasksList` vs. `isTaskDetail` from path-segment count, shows `FilterRow` only on the list shape, and passes `AppBar` a new optional `onBack` (a chevron button — allowed here since a task detail page isn't a tab root, unlike Phase 11's "no back chevron on tab roots" rule for the five sections themselves). **Real bug caught by `impeccable`'s detector before this phase closed:** the first `PullToRefresh` implementation animated the indicator's `height` on spring-back, which `DESIGN.md` §5 explicitly forbids ("transform and opacity only... never height/top/left/width" — the exact anti-pattern the design doc calls out as causing layout thrash on mid-range Android). Fixed by pushing the list content down via `translateY` instead (revealing the spinner behind it, the same technique most native pull-to-refresh implementations use) — re-running the detector after the fix left only the pre-existing, already-approved `overused-font` advisory on Geist (unchanged since Phase 9). **Verified live:** `npm run build` succeeds; `npx oxlint` passes (warnings only, all pre-existing or the standard "hook file exports more than components" advisory already present elsewhere in the codebase); a temporary Playwright pass (`playwright` installed with `--no-save`, removed after) against the dev server, with the same temporary `window.__TEMP_BYPASS_AUTH__` flag from Phase 11 (wired into `Root.jsx`/`ProtectedRoute.jsx` and fully reverted after — no real Clerk credentials available non-interactively) plus full network-level mocking of `/api/tasks*` (no real backend data was exercised; this environment could not run a background dev server this session, unlike the fuller live passes claimed in Phases 9–11, so this was route-mocked rather than against the real Supabase-backed API) confirmed: the Active list renders the correct card count and filters correctly by status; the MISSED section's card reads "Needs review"; the Deleted section's empty state shows the verbatim `DESIGN.md` §7.2 copy; a forced network failure on the Completed list renders the verbatim error copy with a working Retry; task detail loads and shows full info; clicking Edit opens the sheet pre-filled with the task's current values; submitting a `PATCH` closes the sheet and the detail view reflects the new title immediately; the back chevron returns to `/tasks/active`; the bottom nav's Create button opens the sheet, a `POST` submission closes it; Escape closes an open sheet; and a second page context with `reducedMotion: 'reduce'` still rendered the list correctly. Also screenshotted the Active list with a CSS `grayscale(1)` filter applied — priority (rail brightness + uppercase label) and status (word inside the chip) both stayed fully legible with color removed, per the §8 "never color alone" requirement. **Not verified live:** an actual signed-in session hitting the real backend end-to-end with real task data (no real Clerk credentials non-interactively, consistent with every prior phase, compounded this session by the background-dev-server restriction noted above) and real-device 200% OS text-scaling behavior (the `rem`-based type scale and `text-base` 16px input floor are unchanged mechanisms from Phase 9, not re-verified on a physical device).

**Phase:** 11 — Application Shell. Done.
**Overall status:** Built the authenticated app shell exactly to `DESIGN.md` §6: `AppBar` (`client/src/components/nav/AppBar.jsx` — title scale + mono uppercase context line, at most one trailing action slot, no back chevron), `BottomNav` (`client/src/components/nav/BottomNav.jsx` — five slots, 56px + `env(safe-area-inset-bottom)`, `rgba(13,13,13,.86)` backdrop blur with solid fallback, 22px Lucide icons at 1.75px stroke, 10px labels, indigo `layoutId` pill, 40px Steel Teal Create square, amber needs-review dot, 48px tap targets via `--size-tap-nav`), and `FilterRow` (`client/src/components/nav/FilterRow.jsx` — five scroll-snapping status pills with stubbed counts, selected pill scrolled into view on mount via `scrollIntoView`). New `client/src/layouts/AppLayout.jsx` composes AppBar + FilterRow (shown only under `/tasks/*`) + an `AnimatePresence`-wrapped `Outlet`/`children` + BottomNav, and derives each screen's title/context line from `location.pathname` (Home gets today's date via a new `formatTodayContext()` in `lib/datetime.js`; Tasks gets the status label; Insights/Profile get static context). **Routing:** `/tasks/active`, `/tasks/missed`, `/tasks/completed`, `/tasks/incomplete`, `/tasks/deleted` are literal per-status routes (not a single `/tasks/:status` dynamic route) nested under `ProtectedRoute` → `AppLayout` in `routes/index.jsx`, matching the phase's explicit five-separate-files instruction; `AppLayout` reads the selected status straight off the pathname rather than `useParams()` since there's no dynamic segment. `/insights` ships as a styled, locked placeholder (`Insights.jsx`) per DESIGN.md §6. `Root.jsx` now wraps `Home` in `AppLayout` directly (`<AppLayout><Home/></AppLayout>`, using `AppLayout`'s `children` prop) rather than through nested routing, since `Root` — not the router — decides whether the authenticated shell renders at all at `/`; `AppLayout` was written to support both call shapes (`children` falls back to `<Outlet/>`). **Trade-off, logged not fixed:** because Home is reached through `Root` and the other sections through a separate nested-route `AppLayout` instance, navigating from `/` to `/tasks/*` remounts `AppLayout` (and therefore `BottomNav`) once, so the nav pill's `layoutId` travel snaps on that specific transition instead of animating; tab-to-tab navigation within `/tasks/*`, `/insights`, `/profile` (the common case) animates correctly since those all share one persistent `AppLayout` instance. Considered acceptable for this phase's placeholder-content scope; worth revisiting if it reads as a glitch once Phase 12+ content is in place. Stripped the per-page headers `Home.jsx`/`Profile.jsx` used to render themselves (brand mark + Profile link row on Home, "Profile" title on Profile) since `AppBar` now owns the title for every authenticated screen. **Create sheet:** `BottomNav.jsx` includes a minimal inline `CreateSheet` (scrim, `role="dialog"`, `aria-modal`, Escape-to-close, initial-focus + return-focus-to-trigger) purely to prove Create opens in place rather than routing, per the phase's explicit in-scope bullet — this is **not** the full `Sheet` primitive DESIGN.md §7 assigns to Phase 12 (no drag-to-dismiss, no generic focus trap, no TaskForm content); it says as much in its own placeholder copy ("Task creation lands in Phase 12") and isn't exported for reuse. Needs-review badge and FilterRow counts are wired to real props but stubbed at `0`/`{}` respectively, per the phase's explicit "counts may be stubbed" allowance — Phase 12 supplies real numbers. Added `lucide-react` as a real dependency (nav icons; `client/DESIGN.md` §6.1 specifies Lucide at 22px/1.75px stroke). **Verified live:** `npm run build` succeeds; a temporary headless Playwright pass (`playwright` installed with `--no-save`, chromium browser fetched, both removed after) against the dev server, with a `window.__TEMP_BYPASS_AUTH__` flag temporarily wired into `Root.jsx`/`ProtectedRoute.jsx` and reverted immediately after (no real Clerk credentials available non-interactively, same constraint as every prior phase), confirmed: correct AppBar title/context per route (`Home`/`MONDAY 17 AUG`, `Tasks`/`ACTIVE`|`NEEDS REVIEW`|`COMPLETED`|`NOT DONE`|`DELETED`, `Insights`/`LOCKED · PHASE 15`, `Profile`/`ACCOUNT`); BottomNav visible and correctly highlighting the active tab with the indigo pill on every route; the Create sheet opens on click and closes on Escape; zero console errors across all eight routes exercised; unauthenticated access to `/profile` still redirects to `/login` and `/` still shows the real `Landing` page (confirming the temporary bypass didn't leak into the reverted code); a second pass with Playwright's `reducedMotion: 'reduce'` context confirmed nav still functions with motion disabled. `npx impeccable detect src` returned only the pre-existing, already-approved `overused-font` advisory on Geist (unchanged since Phase 9) — no new findings from Phase 11's files. **Not verified live:** an actual signed-in session end-to-end (no real Clerk credentials non-interactively) and real device safe-area rendering on a physical notched phone (verified only via the CSS `env(safe-area-inset-*)` + `pb-safe` utility, unchanged mechanism from Phase 9/10).

**Phase:** 10 — Authentication Frontend (expanded scope: public landing page). Done.
**Overall status:** User approved Phase 10 together with an explicit scope addition: "create a Home page or a starting page too. Before that, the login and then the application" — i.e. a public marketing landing page as the true entry point, ahead of auth. Built as one phase since the landing page is the thing that routes into login/signup. Used `/impeccable` throughout (design guidance, then the mechanical detector as a finishing gate) per the user's explicit instruction, and treated this as an **extension of the already-settled `DESIGN.md` system** (Established world → inherit it), not a new visual-identity exercise — `DESIGN.md`'s "Approved decisions" table (§11) explicitly says changing any of those needs a new conversation, so no tokens, fonts, or color decisions were touched. Skipped Impeccable's full new-work concept-tournament (PRODUCT.md init, `concept-seed.mjs`, image-generation decision page) as disproportionate to a token-constrained extension of an already-exhaustive, already-approved design system; instead composed the landing page directly against `DESIGN.md`'s principles and the `craft-floor.md` quality bar (no icon-grid cards, no kicker/eyebrow labels, no gradient text, no generic hero-metric template). Offering `/impeccable init` remains open per the skill's own guidance for narrow-refinement work. **Routing:** `/` resolves via a new `Root.jsx` — `<SignedOut>` renders the new `Landing.jsx` (public marketing page), `<SignedIn>` renders `Home.jsx` (the existing authenticated placeholder, lightly restyled with a brand mark + Profile link) — chosen over a separate `/home` route so `DESIGN.md` §6's nav table (`/` = Home dashboard slot) needs no rework when Phase 11 builds the real bottom nav. `/login/*` and `/signup/*` (wildcard — Clerk's path-routed `<SignIn>`/`<SignUp>` own sub-steps like `/login/factor-one`) render themed Clerk components on an indigo-wash backdrop (`AuthBackdrop.jsx`, reusing `DESIGN.md` §2.2's ambient-indigo treatment). `/profile` is a genuinely protected route (`ProtectedRoute.jsx`, a layout route using `useAuth()` + `<Navigate>`, not just conditional rendering) — added specifically to give Phase 10 a real protected destination with working sign-out, matching `DESIGN.md` §6's nav table (`/profile` is the real Profile slot) rather than inventing a throwaway route. **Clerk theming:** `client/src/lib/clerkAppearance.js` maps Clerk's `appearance.variables`/`elements` API onto the existing CSS custom properties (`--color-raised` card, `--color-overlay` inputs, `--color-accent-solid` primary button, 16px input font floor per §3.2, 44px button height) — verified visually (screenshots) that no default Clerk purple/default radii/default type survive. **Landing page:** built as the one real proof point rather than generic SaaS marketing copy — `LifecycleDemo.jsx` is a self-looping card that cycles through the actual product mechanism (`ACTIVE` → `MISSED`/"Needs review" → resolved `COMPLETED`), reusing the app's real signature completion moment from `DESIGN.md` §5.4 (scale-press, green wash, SVG checkmark draw) rather than inventing marketing-only motion, with a `useReducedMotion()` branch per §5.6. A "ledger" section demonstrates all five statuses with real `StatusChip`-style 14%-alpha-tint chips (via `color-mix()`, no new hex values) to make "nothing gets deleted from the story" concrete rather than asserted. No fabricated commercial claims (no customer counts, testimonials, or logos) — copy stays scoped to what the product actually does. **Real bug found and fixed, app-wide, not landing-page-scoped:** `client/src/styles/globals.css`'s reset block (`*,*::before,*::after{margin:0;padding:0}` etc., shipped in Phase 9) was **unlayered** plain CSS, and per the CSS Cascade Layers spec, unlayered rules always beat `@layer`-wrapped rules regardless of selector specificity — so it was silently overriding every Tailwind spacing utility (`px-*`, `mt-*`, `gap-*`, ...) with `0`, app-wide, since Phase 9. Diagnosed via computed-style measurement in a headless Playwright check (not visible by inspection — the existing placeholder screens happened not to expose it), confirmed as the cause by wrapping the reset in `@layer base` (and the class-based additions in `@layer utilities`), rebuilding, and re-measuring: `padding-inline` on a `px-gutter` element went from `0px` to the correct `16px`. This means **Phase 9's shipped `Button` component was already silently losing its Tailwind-utility padding/spacing** (it happened to still look plausible because its own explicit height/flex-centering classes masked the missing horizontal padding) — worth a visual re-check next time `Button` is touched. `Button.jsx` also gained an optional `to` prop (renders `motion.create(Link)` instead of `motion.button` when present) so primary/CTA actions that navigate don't need an invalid `<a>`-inside-`<button>` nesting. Added a minimal `Skeleton.jsx` (shimmer block, `DESIGN.md` §5.5's 1.4s sweep — not the full card/list-row variants, which are Phase 12 scope) purely to satisfy this phase's explicit "no flash of protected content, use the skeleton treatment" acceptance criterion in `ProtectedRoute`/`Root`. **Verified live:** `npm run build` succeeds; a headless Playwright pass against the dev server confirmed zero console errors on `/`, `/login`, `/signup`; visiting `/profile` unauthenticated redirects to `/login`; the lifecycle demo correctly cycles through Active (teal) → Needs review (amber) → Completed (green) chip states with the real flourish; `node .claude/skills/impeccable/scripts/detect.mjs` run against every changed/added file returned only the pre-existing, already-approved `overused-font` advisory on Geist (logged and accepted in Phase 9), no new findings. **Not verified live** (no real Clerk credentials available non-interactively, same constraint noted throughout Phases 1/7): actually completing a real sign-up/sign-in round trip and confirming `/api/me` succeeds with a live session token — the wiring itself (Clerk's `getToken()` → `apiClient`'s `Authorization` header) is unchanged from Phase 9, where it was verified end-to-end against the real backend.

**Phase:** 9 — Frontend Foundation. Done.
**Overall status:** Scaffolded the React + Vite frontend at `client/` (`npx create-vite client --template react`, then `npm install react-router-dom framer-motion @clerk/clerk-react` plus `tailwindcss`/`@tailwindcss/vite` as dev deps). **Incident during scaffolding, disclosed to the user immediately:** the scaffold command needed `--overwrite` since `client/` already held `DESIGN.md` and `public/`; this wiped the pre-existing, **uncommitted** `client/public/brand/` (logo mark + lockup SVGs) and the real hand-crafted `client/public/favicon.svg` from disk, replacing them with Vite's placeholder favicon. `DESIGN.md` was recovered via `git checkout` (it had been committed in the "Design System Added" commit); the brand SVGs could not be — they were never committed, and no recovery path existed (checked git history, the `DesignSync` design-system-project list, and published Artifacts). User decided to regenerate/restore those assets themselves rather than have this session attempt a re-creation; `client/index.html` still points `<link rel="icon">` at `/favicon.svg`, so dropping the real file back in that path is a drop-in fix, no code change needed. **Lesson logged as feedback memory:** never run a scaffolding/generator CLI with an overwrite flag against a non-empty target directory without first checking for uncommitted content — stage into a temp dir and merge by hand instead. Built out the full scope: `client/src/styles/tokens.css` implements every token from `DESIGN.md` §2.3–2.5 (surface/ink/status/priority colors), §3.1 (type scale — `--text-*` Tailwind v4 composable font-size tokens with paired line-height/tracking/weight), §4 (spacing/radii/tap targets), and §5.1–5.2 (easing/duration) as a Tailwind v4 CSS-first `@theme` block (`vite.config.js` uses the `@tailwindcss/vite` plugin, no `tailwind.config.js`). `client/src/styles/globals.css` has the reset, `@font-face` declarations, `100dvh`/safe-area/tap-highlight/touch-action mobile hygiene rules, and the `:focus-visible` ring. Geist Sans + Geist Mono (weights 400/500/600 only) self-hosted as real subset `.woff2` files in `client/public/fonts/` — extracted from the `geist` npm package's static per-weight files, then glyph-subset with the `subset-font` npm package (harfbuzz/wasm, no Python/fonttools available in this environment) down to Latin + common punctuation + typographic quote/dash characters, landing at ~120KB total across 6 files (vs. DESIGN.md's ~90KB estimate — close enough for a real subset, not the ~300KB unsubset originals). `client/index.html` has the `viewport-fit=cover`/`interactive-widget=resizes-content` viewport meta and `theme-color=#0D0D0D` per §9. Built `Button` (`client/src/components/ui/Button.jsx`) with all 4 variants (primary/secondary/ghost/destructive) × 3 sizes (sm/md/lg), loading + disabled states, 44px min-height floor, and the focus ring — verified live via a temporary Playwright render (not left in the app; Home stayed a minimal placeholder per scope) at a 390×844 mobile viewport: all variants render with correct token colors, tab-focus shows the visible accent ring, the primary button's bounding box measured exactly 44px tall, and no console errors in either normal or `prefers-reduced-motion: reduce` mode. `client/src/lib/apiClient.js` is a `fetch` wrapper reading `VITE_API_BASE_URL`, throwing a typed `ApiError` with the backend's `{ error: { message, details } }` shape, and exposing `setAuthTokenGetter` so a small `ApiAuthBridge` component in `App.jsx` (using Clerk's `useAuth().getToken()`) can attach the session token to every request without the plain-fetch module needing to call a React hook directly. `client/src/lib/motion.js` has shared Framer variants (route cross-fade, list stagger, press feedback) with explicit reduced-motion branches per §5.6, and `client/src/lib/datetime.js` has UTC→local relative/absolute formatting per §9 (`Intl.RelativeTimeFormat`/`Intl.DateTimeFormat`, no date library). `App.jsx` wraps everything in `ClerkProvider` (from `VITE_CLERK_PUBLISHABLE_KEY`) → `MotionConfig reducedMotion="user"` → `BrowserRouter`; `client/src/routes/index.jsx` has one placeholder `/` route (`Home.jsx`) that live-fetches `/api/health` as an end-to-end smoke test. Created `client/.env` (gitignored — `client/.gitignore` had no `.env` entry out of the Vite template box, added one) reusing the real Clerk **publishable** key already present in `server/.env` (safe to reuse client-side; publishable keys are meant to be public) alongside `VITE_API_BASE_URL=http://localhost:8080/api`. Verified live end-to-end with both servers running: backend `/api/health` returns 200, frontend dev server serves the placeholder page showing "API: ok", production `npm run build` succeeds (321KB JS / 17.5KB CSS, both gzipped), and `npx impeccable install` was run in `client/` (installs as a cross-harness agent skill, not a package dependency — also dropped a `.codex/hooks.json` in the repo root since it detected a local Codex CLI install; harmless, left as-is) followed by the `audit` command against the scaffold: zero blocking findings, only advisory-severity `overused-font` warnings on Geist Sans/Mono, which is a settled, explicitly-approved `DESIGN.md` §11 decision, not a real issue. No task UI, auth UI, or real app layout was built — scope stayed at scaffolding per the phase prompt.

**Phase:** 8 — Backend Completion & API Documentation. Done. **Backend milestone complete — ready for frontend integration (Phase 9).**
**Overall status:** Wrote `server/API.md` documenting every endpoint (`GET /api/health`, `GET /api/me`, and all 7 `/api/tasks*` routes) — method, auth requirement, request body/query/param shape, success response + status code, and every error response + status code, plus the task object shape, the error-response shape, a status-code reference table, the automatic-missed-detection behavior, and every env var (cross-referencing `example.env` rather than duplicating values). Final consistency pass found and fixed one real inconsistency: `GET /api/me` returned `{ clerkUserId }` (camelCase) while every task field is `snake_case` (mirroring DB columns directly) — changed to `{ clerk_user_id }` so the entire API is consistently `snake_case`. Status codes confirmed consistent across every endpoint and error type per the table now in `API.md` (400/401/404/409/500, plus 201 for creation) — no changes needed there, Phase 4's design already held up. Performed a final manual end-to-end lifecycle walkthrough (create → edit → complete; create → delete; create-with-past-deadline → GET triggers auto-MISSED → resolve-missed INCOMPLETE; create-with-past-deadline → resolve-missed COMPLETED with lazy pre-transition; list + status filter) against the real running app and real Supabase, using the same Clerk-mocking technique as the Phase 7 suite (still no real Clerk token available non-interactively) — run as a one-off temporary Vitest file, all assertions passed, full response bodies printed and reviewed, temp file and test data deleted afterward. Reviewed `server/package.json` scripts (`start`, `dev`, `db:smoke-test`, `test`, `test:watch`) — complete and correctly named; no `migrate` script exists by design, since migrations are applied by hand through the Supabase SQL editor per the Phase 2 decision (no Node/`pg`-based migration runner), not an oversight. Full test suite re-run after the `/api/me` fix: still 73/73 passing. No new features/endpoints added, no infrastructure changes — this was documentation, consistency, and verification only, as scoped.

**Phase:** 7 — Backend Testing. Done.
**Overall status:** Built an automated Vitest + Supertest suite (`server/tests/`, 71 tests across 6 files) covering everything Phases 1-6 shipped: auth (401 on every task route unauthenticated), cross-user authorization (404s identical in shape for foreign vs. nonexistent ids, no field/state leakage after rejected attempts), CRUD (creation validation, listing/filtering by owner, get-by-id, editing rules including system-field lockdown and non-ACTIVE rejection), completion, deletion (reason requirement, soft-delete, terminal-state rejection), the Phase 6 automatic MISSED detection (lazy-on-read for both list and single-get, idempotent on re-read, COMPLETED/DELETED tasks never touched, no client path to set MISSED directly, no `/miss` endpoint, `sweep-missed` bulk transition), the full `resolve-missed` contract (409 unless currently MISSED, 400 on missing/invalid/empty-reason resolution, both INCOMPLETE/COMPLETED branches with correct field preservation, terminal after either), and `?status=` filtering across all five statuses. **Auth mocking:** `@clerk/express` is fully mocked via `vi.mock` in `server/tests/setup.js` (a Vitest `setupFiles` entry, loaded before any test file) — the mock middleware reads a `x-test-user-id` request header and sets `req.auth = { userId }`, and mocked `getAuth` reads it back; tests authenticate by setting that header via the `asUser(clerkUserId)` helper in `server/tests/helpers/client.js`, so the suite never makes a real Clerk network call. **Test database strategy:** tests run against the same real Supabase project as dev (no separate test project/schema was provisioned — this session has no interactive path to `supabase login`/`supabase link` or the dashboard, same constraint noted in Phase 2's migration approach), but every test user gets a globally-unique `test-clerk-<uuid>` Clerk id (`newClerkUserId()` in `client.js`), so runs never collide with each other or with real data; `server/tests/helpers/db.js`'s `cleanupUser()` deletes a test user's tasks then the user row directly via `@supabase/supabase-js` (bypassing the API, since terminal-state tasks can't be deleted through it) in each file's `afterAll`. Verified live after a full suite run that zero `test-clerk-%` rows remain in `users`. This is real create+cleanup per run, not transactional rollback (no direct Postgres connection is available to roll back within, per the project's no-`pg` rule). Added `vitest`/`supertest` as devDependencies, `server/vitest.config.js` (node environment, `setupFiles: ['./tests/setup.js']`, 20s test timeout to tolerate real cloud round-trips), and `npm test`/`npm run test:watch` scripts. All 71 tests pass. No CI/CD pipeline was set up (out of scope per the phase prompt).

**Post-Phase-7 correction (user asked "any gaps or bugs?" after Phase 7 was marked Done):** a targeted review turned up two real gaps the test suite didn't cover, both fixed immediately. (1) **Bug:** a malformed JSON request body (e.g. `POST /api/tasks` with invalid JSON) was returning `500 "Internal server error"` instead of `400` — `express.json()` throws a `SyntaxError` that isn't an `AppError`, so `errorHandler` (`server/src/middleware/errorHandler.js`) fell through to its generic 500 branch. Fixed by special-casing `err.type === 'entity.parse.failed'` (body-parser's error type) before the generic branch, returning `400 "Malformed JSON in request body"`. (2) **Minor gap:** unmatched routes (e.g. `GET /api/nonexistent`) returned Express's default HTML 404 page instead of the API's JSON error shape, inconsistent with every real endpoint. Fixed by adding a catch-all JSON 404 handler in `server/src/app.js`, mounted after all routers and before `errorHandler`. Both verified live via curl and covered by a new regression test file, `server/tests/app.errors.test.js` (2 tests) — full suite is now 73 tests, still all passing, still zero leftover `test-clerk-%` rows after a run.

---

## Phase Status Table

| Phase | Name | Status |
|-------|------|--------|
| 0 | Project Foundation | Done |
| 1 | Authentication | Done |
| 2 | Database | Done |
| 3 | Core Task REST API | Done |
| 4 | Validation & Error Handling | Done |
| 5 | Authorization & Security | Done |
| 6 | Automatic Task Status Handling | Done |
| 7 | Backend Testing | Done |
| 8 | Backend Completion & API Documentation | Done |
| 9 | Frontend Foundation | Done |
| 10 | Authentication Frontend | Done |
| 11 | Application Shell | Done |
| 12 | Task Management UI | Done |
| 13 | Task Lifecycle UI | Done |
| 14 | Dashboard | Done |
| 15 | Analytics | Done |
| 16 | Frontend Testing & Polish | Done |
| 17 | Notifications & Email | Done |
| 18 | Push Notifications & In-App Feed | Done |
| 19 | Time Tracking | Done |
| 20 | Productivity Analytics 2.0 | Done |

Status values: `Not started` → `Prompt ready` → `Approved` → `In progress` → `Done`.

---

## Decisions Log

- **2026-08-17, Phase 0:** Server uses ESM (`"type": "module"` in `server/package.json`), not CommonJS.
- **2026-08-17, Phase 0:** Entrypoint split into `server/src/index.js` (env load + `app.listen`) and `server/src/app.js` (Express app/middleware/route wiring), for testability in later phases.
- **2026-08-17, Phase 0:** `PORT` falls back to `8080` if unset in env.
- **2026-08-17, Phase 1:** Used `@clerk/express` (`clerkMiddleware()` + `getAuth()`), not the deprecated `requireAuth()` helper — `requireAuth()` redirects to a sign-in URL on failure rather than returning JSON, which doesn't fit an API-only backend. Auth is enforced via a small custom `requireAuthenticated` middleware (`server/src/middleware/auth.js`) that checks `getAuth(req).userId` and returns a generic `401` JSON error if absent.
- **2026-08-17, Phase 1:** `withClerkAuth` (`clerkMiddleware()`) is applied globally in `app.js` (before route mounting) so `getAuth(req)` is available anywhere; `requireAuthenticated` is applied per-route (currently only `/api/me`) so unauthenticated routes like `/api/health` stay open.
- **2026-08-17, Phase 1:** No `users` table lookup yet — `/api/me` returns the raw Clerk user id straight from the verified session, per Phase 1 scope. Mapping to internal `users.id` via `users.clerk_user_id` happens starting Phase 2.
- **2026-08-17, Phase 2:** `status` and `priority` enforced via Postgres `CHECK` constraints (not enum types) — simpler to alter later (`ALTER TABLE ... DROP/ADD CONSTRAINT`) than `ALTER TYPE`, and the value sets are still fully controlled by `CLAUDE.md`.
- **2026-08-17, Phase 2:** Migration applied by hand through the Supabase SQL Editor, not `supabase db push` — the Supabase CLI's `db push` requires an interactive `supabase login` + `supabase link` (project access token / DB password), which isn't available non-interactively in this session. Migration SQL still lives in the repo at `server/supabase/migrations/` for review, per the Supabase CLI's own file convention.
- **2026-08-17, Phase 2:** `supabase/migrations/` lives under `server/supabase/migrations/`, not at the repo root, per user preference (keeps all backend-owned artifacts under `server/`).
- **2026-08-17, Phase 2:** Connectivity smoke test lives at `server/src/db/smokeTest.js` (run via `npm run db:smoke-test` in `server/`) rather than a temporary route — a one-off script that queries `users` through `@supabase/supabase-js`. It's a real script kept in the repo, not something to strip before merge.
- **2026-08-17, Phase 3:** Repositories use the `@supabase/supabase-js` query builder exclusively (`.eq()`, `.select()`, etc.) — not raw parameterized SQL over a connection pool, despite that wording in `prompts/phase-03.md`. That prompt predates the Phase 2 decision to drop `pg`/direct Postgres entirely; `CLAUDE.md`'s "no `pg`, no raw SQL" rule governs and takes precedence over stale prompt wording.
- **2026-08-17, Phase 3:** Foreign/nonexistent task access returns `404` (not `403`) for all task endpoints — chosen for consistency and to avoid leaking task existence to non-owners.
- **2026-08-17, Phase 3:** `usersRepository.findOrCreateByClerkUserId` handles a concurrent-first-request race (two simultaneous requests both missing the `users` row) by catching the Postgres unique-violation (`23505`) on insert and re-fetching, rather than erroring.
- **2026-08-17, Phase 3:** `errorHandler` now passes through `err.message` for client errors (`status < 500`) instead of always returning a generic "Internal server error" — needed so lifecycle/validation rejections (invalid transition, missing reason, etc.) surface as clear 4xx errors per Phase 3's acceptance criteria. 5xx responses still hide internal details, unchanged from Phase 0. (Superseded by Phase 4's typed-error-based `errorHandler`, see below.)
- **2026-08-17, Phase 4:** Error shape is `{ "error": { "message": "...", "details"?: [{ "path": "...", "message": "..." }] } }`. `details` is present only for `ValidationError` (400) and omitted otherwise, so the shape stays consistent without an empty array on every response.
- **2026-08-17, Phase 4:** Status codes settled: `400` validation, `404` not-found/not-owned (still consistent with Phase 3's choice), `409` invalid state transition, `401` unauthenticated (via the existing Phase 1 `requireAuthenticated`, which still short-circuits before validation middleware runs — auth is checked first). 500s log the full error server-side via `console.error` and return only a generic message.
- **2026-08-17, Phase 4:** `createTaskBodySchema` and `editTaskBodySchema` use Zod `.strict()` to explicitly reject unknown keys (e.g. `status`, `user_id`, `missed_reason`) with a 400, rather than Phase 3's approach of silently dropping them. Tightens CLAUDE.md's "client must never be able to set these fields" rule from silent-ignore to explicit-reject.
- **2026-08-17, Phase 4:** `editTaskBodySchema` requires at least one of `title`/`description`/`deadline`/`priority` via `.refine()` — an empty PATCH body is now a 400 instead of a silent no-op update.
- **2026-08-17, Phase 4:** `idParamSchema` validates `:id` as a UUID (matching Phase 2's `uuid` primary key choice) — malformed ids are now a clean 400 instead of risking an unhandled Postgres `22P02` error surfacing as a 500.
- **2026-08-17, Phase 4:** Business-rule validation (state-transition checks, not-found lookups) stays in `tasksService.js`; field-shape/type/format validation (required fields, enums, UUID format, non-empty trimmed strings) moved entirely to Zod schemas run by route-level middleware, so services no longer duplicate manual checks from Phase 3.
- **2026-08-17, Phase 5:** Security audit of Phases 1–4 found the implementation already sound on ownership scoping, cross-user isolation, system-field lockdown, auth coverage, and SQL parameterization — no code changes needed there. The one real gap was CORS: `app.use(cors())` with no options reflects/allows any origin. Fixed in `server/src/app.js` with an explicit allowlist read from a new `CORS_ORIGIN` env var (comma-separated, defaults to `http://localhost:5173`) plus `credentials: true`, since Clerk sessions can ride on cookies cross-origin. `CORS_ORIGIN` documented in `example.env` and set in `server/.env`.
- **2026-08-17, Phase 5:** Confirmed (not just by inspection but live requests against the running app) that 404 is returned identically for a genuinely nonexistent task id and for another user's task id — both paths go through `tasksService.getOwnedTaskOrThrow`, which is the sole gate before any read or mutation, so there is no separate "exists but not yours" code path that could leak via timing, status code, or message differences.
- **2026-08-17, Product decision (pre-Phase 6, docs only — no code changed):** Corrected the meaning of `MISSED`. It no longer means "the user never completed this task" — it means only "the deadline passed without the system receiving a completion confirmation." An overdue ACTIVE task auto-transitions to `MISSED` (unchanged mechanism from the original Phase 6 design: lazy check on read, plus an optional non-scheduled `sweepMissedTasks`), but MISSED is now a checkpoint the user must resolve, not a final verdict. Added `POST /api/tasks/:id/resolve-missed` (body `{ "resolution": "INCOMPLETE" | "COMPLETED" }`, authenticated + ownership-scoped, only callable on a MISSED task) as the resolution mechanism: `INCOMPLETE` keeps the task `MISSED` (terminal) and preserves the original `missed_reason`/`missed_at`; `COMPLETED` moves it to `COMPLETED`, sets `completed_at`, updates `updated_at`, and deliberately leaves `missed_at`/`missed_reason` un-cleared so the task's missed history survives the correction — no schema change needed for that, since those columns already exist and nothing required nulling them on a further transition. As part of this, **removed the manual `POST /api/tasks/:id/miss` endpoint from the design entirely** — under the corrected model, a user cannot self-declare a task "missed" (that would just be a same-effort path to DELETED with extra steps); MISSED is exclusively system-detected. Updated `prompts/phase-03.md` (drops the `/miss` endpoint and the `ACTIVE→MISSED` transition from its scope — Phase 3 now only ships `ACTIVE→COMPLETED`/`ACTIVE→DELETED`), `prompts/phase-06.md` (full rewrite around the corrected lifecycle and the `resolve-missed` contract), `prompts/phase-07.md` and `prompts/phase-13.md` (not-yet-built phases whose test/UI plans referenced the old manual-miss-with-reason flow), `CLAUDE.md` (API convention, lifecycle rules, testing coverage), and `PHASES.md` (Phase 3/6/13 summaries). Phases 3 and 6 remain unimplemented/not-done — this was a documentation-only correction made before Phase 6 is built; Phase 3's already-shipped code (which still has the old `/miss` endpoint) will need updating when Phase 6 is actually implemented.

- **2026-08-17, Product decision refinement (pre-Phase 6, docs only — no code changed): `INCOMPLETE` split out as its own status, distinct from `MISSED`.** Follow-up to the decision above. `MISSED` is purely a transient, pending-resolution checkpoint ("deadline passed, not yet confirmed either way") — it should never be the resting place for a task the user has actually confirmed was never done. That confirmed-not-done outcome is now a separate terminal status, `INCOMPLETE`, reached only via `resolve-missed`. Changed `resolve-missed`'s contract: `{ "resolution": "INCOMPLETE", "reason": "..." }` (reason now required, non-empty, same rule as delete's reason) transitions `MISSED → INCOMPLETE`, not "stays MISSED" as the previous iteration had it; `{ "resolution": "COMPLETED" }` (no reason) is unchanged, `MISSED → COMPLETED`. This needs a real schema change (`status` CHECK constraint widened to add `INCOMPLETE`; new `incomplete_reason`/`incomplete_at` columns on `tasks`) — `missed_reason`/`missed_at` stay exactly as they were (the automatic detection record) and are never repurposed to hold the user's own reason; the two reason/timestamp pairs are independent and both preserved, so a task's full history (auto-detected → user's own explanation) is reconstructable. The migration is scoped into Phase 6 (which is unbuilt) rather than reopening the already-Done Phase 2; `prompts/phase-02.md` got a short forward-pointing note instead of a rewrite. Updated for consistency: `prompts/phase-06.md` (full rewrite: migration, new status, resolve-missed contract), `prompts/phase-03.md` (status/filter notes), `prompts/phase-07.md` (test coverage), `prompts/phase-11.md`/`phase-12.md`/`phase-13.md`/`phase-14.md`/`phase-15.md` (frontend: a separate "Incomplete" section/tab distinct from "Missed," reason-collection UI on the INCOMPLETE resolve path only, dashboard/analytics counts and breakdowns), `CLAUDE.md` (data model, API convention, lifecycle rules, testing, security reason-rejection rule), and `PHASES.md` (phase summaries). As before, no phase status changed and no code was written — Phase 2 and Phase 3 remain Done against the old (pre-`INCOMPLETE`) schema/API; both will need real changes (migration + endpoint rework) once Phase 6 is actually implemented.
- **2026-08-17, Phase 7:** `@clerk/express` is mocked wholesale with `vi.mock` (`server/tests/setup.js`, loaded via Vitest's `setupFiles`) rather than using real Clerk test-mode tokens — the mock middleware trusts a test-only `x-test-user-id` header and sets `req.auth = { userId }`, matching the shape `requireAuthenticated`/`getAuth` already expect. Chosen because no real Clerk credentials/network access is available non-interactively in this environment, and because unit/integration tests shouldn't depend on a third-party auth network call for speed and reliability regardless.
- **2026-08-17, Phase 7:** No separate test Supabase project/schema — tests run against the same live project as dev, since this session has no interactive path to provision or link a second one (`supabase login`/dashboard access needed). Isolation instead comes from giving every test run globally-unique `test-clerk-<uuid>` Clerk ids and cleaning up (deleting each test user's tasks, then the user row) directly via `@supabase/supabase-js` in `afterAll` hooks. This is real create+cleanup per run, not transactional rollback — a rollback strategy would need a direct Postgres connection to run tests inside, which the project's no-`pg` rule rules out. If a dedicated test project becomes available later, only `server/tests/helpers/db.js`'s cleanup and the Supabase client's env vars would need to change — no test logic depends on which project it's talking to.
- **2026-08-17, Phase 2/3 correction (explicitly requested, ahead of Phase 6): brought the already-Done Phase 2 schema and Phase 3 API in line with the corrected lifecycle above.** This is a targeted patch to already-shipped code, not a reopening of those phases' full scope. **Phase 2:** applied `server/supabase/migrations/20260817130000_add_incomplete_status.sql` to the live Supabase project by hand through the SQL Editor (same approach as the original Phase 2 migration — CLI push isn't usable non-interactively here) — widens `tasks_status_check` to `ACTIVE|COMPLETED|MISSED|INCOMPLETE|DELETED`, adds nullable `incomplete_reason text` and `incomplete_at timestamptz` columns. Verified live against the real database (not just by inspection): an insert with `status='FOO'` is still rejected by the check constraint, and an insert with `status='INCOMPLETE'` plus `incomplete_reason`/`incomplete_at` now succeeds; test row and any temp user cleaned up after verification. **Phase 3:** removed the manual `POST /api/tasks/:id/miss` endpoint end-to-end — route in `server/src/routes/tasks.js`, controller `tasksController.miss`, and service `tasksService.missTask` all deleted; `reasonBodySchema` in `server/src/validation/taskSchemas.js` renamed to `deleteReasonBodySchema` since it's now delete-only (resolve-missed gets its own schema when Phase 6 is built). Verified the server still boots cleanly and `/api/health` returns 200 after the change. Phase 6 (not yet started) is still where `resolve-missed`, the automatic `ACTIVE→MISSED` detection, and the `?status=INCOMPLETE` filter support get added — this patch only removed the now-invalid manual-miss path and made the DB able to store `INCOMPLETE` rows. Phase 2 and Phase 3 remain marked `Done` in the status table (this was a correction to already-Done work, not a new phase completion).

- **2026-08-17, Design system established (docs only — no code written, no phase started).** User supplied an approved color palette (Charcoal / Japanese Indigo / Steel Teal / Mid Grey / Foggy / Star Dust) and asked for a full mobile design system before frontend work begins. Created **`client/DESIGN.md`** as the authoritative design reference for Phases 9–16 — color tokens with verified WCAG contrast ratios, typography, spacing/radii/elevation, motion, navigation, component inventory, copy voice, accessibility criteria, and a mobile production checklist. Key design decisions, all approved: **dark theme only in v1** (tokens structured for a later light swap); **Geist Sans + Geist Mono** self-hosted, three weights only, with mono reserved for deadlines/timestamps/counts; **five-slot bottom navigation** (Home · Tasks · Create · Insights · Profile) with **no hamburger or drawer anywhere** — the five task statuses are presented as filters behind one Tasks tab while all six routes stay real and deep-linkable; **Tailwind v4 with CSS-first `@theme`** (supersedes the v3-shaped `tailwind.config.js` wording in `prompts/phase-09.md`); **red is bound to the destructive delete *action* only, never to the resting `DELETED` status** (which is receded grey); **`MISSED` renders as an amber "Needs review" chip and `INCOMPLETE` as a Foggy "Not done" chip** — API status names unchanged, but the UI states the action required rather than passing a verdict, per this file's lifecycle rules; **priority is monochrome** (a luminance-encoded 3px rail plus a text label) so it never competes with status color and survives greyscale. Also established: no component may contain a hex value (tokens only), and nothing sourced from 21st.dev MCP ships with its own colors/radii/spacing/type — it is retokenized in the same commit. `npx impeccable install` runs in `client/` immediately after the Vite scaffold in Phase 9 (it needs a `package.json`, so it cannot run against the current `.gitkeep`-only directory); it is a design-quality gate, not a component library, and its findings are blocking acceptance criteria at the close of every frontend phase. **Prompt files updated for consistency:** `prompts/phase-09.md` (token/font/impeccable/Tailwind-v4 scope), `phase-10.md` (Clerk `appearance` theming), `phase-11.md` (nav pattern is now specified rather than a judgement call), `phase-12.md` (monochrome priority replaces the color-coded priority badge; verbatim empty-state copy; 21st.dev retokenization rule), `phase-13.md` (verbatim resolve/reason copy, the completion animation sequence, optimistic updates; `ResolveMissedModal`/`DeleteModal` renamed to `ResolveSheet`/`DeleteSheet` to match the bottom-sheet primitive), `phase-14.md` (`StatTile`, mono tabular numerals), `phase-15.md` (chart layer is a design deliverable to be written back into `DESIGN.md`; route corrected to `/insights`, which Phase 11 already creates — no nav changes needed), `phase-16.md` (full `DESIGN.md` audit as a gate; stale `MissModal` reference removed — there is no manual-miss flow). `CLAUDE.md`'s "Where to Look" now points at `client/DESIGN.md` and states the precedence between the two. **Known gaps to resolve before the phases that need them:** the 21st.dev MCP server is not currently connected, and the Geist font subsets still need adding to `client/public/fonts/`. No phase status changed; Phase 9 remains unapproved and unstarted.

- **2026-08-17, Brand mark imported (assets only — no phase started, no application code written).** Imported the approved logo from the Claude Design project `Chase Logo Final.dc.html` (project `2815f836-e30f-4866-9dd8-7b9b2df141e5`) via the `claude_design` MCP and extracted the inline SVG artwork into standalone files under `client/public/brand/`: `chase-mark-on-dark.svg`, `chase-mark-on-light.svg`, `chase-lockup-on-dark.svg`, `chase-lockup-on-light.svg`, plus `client/public/favicon.svg` (the mark on a `#0D0D0D` rounded badge, at the same 72%-of-badge proportion the design's own 16/20/28/40/64px legibility check used). The mark is a `-9°` leaning hourglass with a Steel Teal lower bulb; every color is verbatim from `DESIGN.md` (`#F2EDEA`/`#6C9BAD` on dark, `#1A1A1A`/`#517380` on light, `#0D0D0D` badge) — no new values introduced. `-on-dark`/`-on-light` names the **background**, not the artwork. `client/public/brand/README.md` documents usage rules and the two known caveats: the lockup's "Chase" wordmark is live `<text>` rather than outlines (so it needs Geist Sans 600, which Phase 9 self-hosts — export a PNG for font-uncontrolled contexts), and raster fallbacks (`favicon.ico`, `apple-touch-icon.png`, maskable PWA icons) still need generating from `favicon.svg`. All five SVGs verified well-formed. **Deferred to Phase 9:** wiring `<link rel="icon" type="image/svg+xml" href="/favicon.svg">` and `<meta name="theme-color" content="#0D0D0D">` into `client/index.html` — that file doesn't exist yet, since the Vite scaffold is Phase 9 scope. `client/public/` was chosen because Vite serves it from the site root, so the assets need no move once the scaffold lands. No phase status changed; Phase 9 remains unapproved and unstarted.

- **2026-08-17, Phase 9:** Router is React Router (`react-router-dom` v7, `BrowserRouter`) — no stated user preference, chosen as the ecosystem-standard default for a Vite SPA with Clerk. HTTP client is a native `fetch` wrapper (`client/src/lib/apiClient.js`), not a library, per the phase prompt's explicit preference to avoid one unless needed — the API surface is small enough that a thin wrapper covers it. Tailwind v4 confirmed via `@tailwindcss/vite` + CSS-first `@theme` in `tokens.css`, no `tailwind.config.js`, per `DESIGN.md` §11. Font subsetting: extracted static per-weight `.woff2` files (400/500/600 only, both faces) from the `geist` npm package, then real glyph-subset (Latin + common punctuation/typographic characters) via the `subset-font` npm package (harfbuzz/wasm — no Python/`fonttools` available in this environment), landing at ~120KB total across 6 files.
- **2026-08-17, Phase 9 incident (disclosed live, not discovered after the fact):** scaffolding `client/` with `npx create-vite client --template react` required `--overwrite` since the directory wasn't empty (`DESIGN.md`, `public/` already existed). That flag wiped `client/public/brand/` (logo mark + lockup SVGs) and the real `client/public/favicon.svg` from disk — both were uncommitted work from the pre-Phase-9 "brand mark imported" session and had no git history to restore from. `DESIGN.md` was committed and recovered cleanly via `git checkout`. User chose to restore the brand assets themselves rather than have this session regenerate them from the `DESIGN.md` spec. **Until they're restored, `client/public/` only has Vite's placeholder `favicon.svg`** — `client/index.html`'s `<link rel="icon">` already points at the right path, so dropping the real files back in is a no-code-change fix.
- **2026-08-17, Brand mark re-imported (assets only, post-Phase-9 cleanup — no phase status changed).** User re-sourced the logo directly from its original Claude Design project ("Chase Logo Design Brief", `2815f836-e30f-4866-9dd8-7b9b2df141e5`, file `Chase Logo Final.dc.html`) via the `claude_design`/`DesignSync` MCP, resolving the Phase 9 incident above. Re-extracted the exact SVG artwork (a `-9°` leaning hourglass, Star Dust top / Steel Teal bottom bulb — every color read verbatim from the source file, matching `DESIGN.md` exactly) into `client/public/brand/`: `chase-mark-on-dark.svg`, `chase-mark-on-light.svg` (standalone icon), `chase-lockup-on-dark.svg`, `chase-lockup-on-light.svg` (icon + live "Chase" `<text>` wordmark, Geist Sans 600), plus `client/public/favicon.svg` (mark on a `#0D0D0D` rounded badge at the same ~72%-of-badge proportion the source brief's 16–64px legibility check used). All five verified by rendering in a headless browser and screenshotting — correct colors, correct proportions, wordmark positioned correctly. `client/public/brand/README.md` documents provenance and the same two caveats as before (live-text wordmark needs Geist Sans loaded; raster fallbacks like `favicon.ico`/maskable PWA icons still ungenerated). No code changes needed to wire it up — `client/index.html`'s `<link rel="icon" href="/favicon.svg">` from Phase 9 already pointed at the right path. **Also cleaned up the repo root at the user's request:** deleted `.codex/` (a `hooks.json` that `npx impeccable install` had dropped at the repo root during Phase 9 after detecting a local Codex CLI install — unused, not part of this project's toolchain) and moved `.claude/` (session-local permission settings, untracked/gitignored, no skills or memory content) from the repo root into `client/.claude/`. Repo root now holds only `CLAUDE.md`, `STATE.md`, `PHASES.md`, `example.env`, `prompts/`, `client/`, `server/`, and `.git`.

- **2026-08-17, Phase 13:** Complete's confirmation is a lightweight `Sheet`-based dialog with plain "Cancel"/"Complete" buttons rather than a full reason-collection flow — the phase prompt explicitly calls Complete low-risk and reversible in spirit, so it gets the same dialog *mechanism* as Delete/Resolve (for consistent accessibility semantics) but none of their reason-gathering weight. Delete and Resolve's reason fields share one `ReasonField` component and one `isReasonValid` (trim-then-check) rule, so "submit disabled while empty" can't drift between the two flows. One `useTaskLifecycle` hook backs Complete/Delete/Resolve on both `TaskCard` and `TaskDetail` — network call and the animation's minimum duration run concurrently rather than sequentially, so neither a fast response nor a slow one breaks the choreography; a card is only actually removed from its list once the server confirms (via a new `removeLocal` on `useTaskList`), so a rejection needs no "undo the removal" step — it only ever needs to reset the still-present card's own visual stage back to idle.
- **2026-08-17, Phase 12:** Deadline/date input is a native `<input type="datetime-local">` (no date-picker library) — `lib/datetime.js` grew `toDatetimeLocalValue`/`fromDatetimeLocalValue` to bridge it to the API's ISO UTC strings, matching that file's existing "UTC → local formatting" scope rather than adding a new module. Priority is a hand-rolled three-button `role="radiogroup"` segmented control, not a `<select>`, so all three options and the currently-selected one are visible without an extra tap. No new tokens were added to `tokens.css` — every Phase 12 component expressed fully in the existing set. 21st.dev MCP remained unconnected this session (flagged since pre-Phase-9), so component structure was hand-built rather than sourced-then-retokenized; nothing hit DESIGN.md §10.4's "stop and raise it" case for an inexpressible component.
- **2026-08-17, Phase 14:** Dashboard aggregation (status counts, recent activity, due-soon) is entirely client-side, computed in `lib/taskStats.js` from one unfiltered `GET /api/tasks`, not a new backend endpoint — the phase prompt explicitly scoped it this way ("avoid adding new backend aggregation endpoints... revisit if performance becomes a real issue"), and at this data scale (a single user's task list, no pagination anywhere yet) one full fetch tallied in-memory costs nothing meaningful. `useDashboardTasks` is a separate hook from the existing `useTaskCounts` (not a reuse) specifically because `useTaskCounts`'s counts are decorative chrome elsewhere and its hook deliberately swallows fetch errors — the dashboard needed real `loading`/`error` states to satisfy DESIGN.md §7.1, so sharing would have meant either breaking that existing silent-failure contract or bolting an error channel onto a hook whose whole point was not having one. "Due soon" only surfaces ACTIVE tasks with a deadline still ahead (within 24h) — an already-overdue ACTIVE task never reaches this component, since the same unfiltered fetch that feeds the dashboard already lazily transitions it to MISSED server-side first (CLAUDE.md's automatic-missed-detection rule), so there's no risk of double-signaling the same task as both "due soon" and "needs review."

- **2026-08-17, Phase 15:** Two scope decisions confirmed with the user before starting, per the phase prompt's explicit "confirm scope with the user" instruction. (1) **Aggregation stays client-side** — same approach as Phase 14's dashboard, no new backend endpoint, over the same `useDashboardTasks`-fetched array. (2) **Charts render via Recharts** (real dependency, added to `client/package.json`), not hand-rolled SVG — user's explicit choice, "suitable colors" retokenized against the existing palette in the same commit, per §10.4's rule for any sourced/library UI. (3) **Chart series colors reuse existing status/priority/accent tokens rather than a new categorical palette** — running the `dataviz` skill's palette validator against a candidate 3-hue set drawn from Steel Teal/Japanese Indigo/Foggy (as the phase prompt suggested) failed the chroma-floor and lightness-band checks, because the app's whole palette is deliberately low-chroma/muted (that's the design identity, not an accident — see the "Design system established" entry above). Rather than fight that by introducing new, more-saturated chart-only colors, every chart reuses the exact hues already carrying the relevant meaning elsewhere in the app (status colors for trends, priority rail colors for the priority breakdown, one flat status hue for ranked reason bars) — see `DESIGN.md` §7.4 for the full writeup and rationale per chart.

- **2026-08-18, Phase 16:** Test runner is **Vitest + React Testing Library**, matching the phase prompt's explicit "Vitest is already used on the backend — reuse it" instruction; jsdom (not happy-dom) chosen for parity with the backend's Node-based tooling assumptions and because Recharts/Framer Motion's browser-API touchpoints (`ResizeObserver`, `matchMedia`, `getBoundingClientRect`) are well-documented shim targets under jsdom. Test files are colocated in `__tests__/` folders next to the components they cover (matching the existing `server/tests/` convention of grouping by concern, adapted to React's common colocation pattern) rather than one flat top-level test directory, except integration/auth-flow tests which live under `src/test/integration/` and `src/routes/__tests__/`+`src/components/auth/__tests__/` since they span multiple components rather than belonging to one.
- **2026-08-18, Phase 16:** `@clerk/clerk-react`'s hooks (`useAuth`, `useUser`) are mocked per-test-file with `vi.mock`, the same approach `server/tests/setup.js` takes for `@clerk/express` — chosen over attempting a real Clerk test-mode session (no credentials available non-interactively, consistent with every prior phase) and over a single global mock (different tests need different `isLoaded`/`isSignedIn` states, which a per-file mock function makes trivial to vary per test via `mockReturnValue`).
- **2026-08-19, Phase 17:** `TASK_MISSED`/`DEADLINE_REMINDER` are deliberately never sent from the existing lazy `ACTIVE→MISSED` check (`tasksService.maybeTransitionToMissed`, which runs on every `GET /api/tasks*`) — a background side effect like sending an email doesn't belong inside a read request. Instead a new scheduled sweep (`jobs/notificationScheduler.js`, plain `setInterval`, no Redis/Kafka/queue) is the sole trigger for both, using a new cross-user `tasksRepository.sweepAllUsersMissed` (no `user_id` filter — the one deliberate exception to this project's usual per-user query scoping, safe only because it's server-internal, never exposed via a route, and runs on the service-role client).
- **2026-08-19, Phase 17:** Duplicate-prevention and restart-safety both come from a real database constraint, not in-memory state — `notification_log`'s `unique (task_id, type, dedup_key)` is what `notificationsRepository.claim()` relies on (insert-first, treat a `23505` conflict as "already claimed, skip"), so a server restart mid-sweep just resumes correctly from what the table already records.
- **2026-08-19, Phase 17:** Fetching a user's email from Clerk is not a Supabase call, so it doesn't belong in `usersRepository` per `CLAUDE.md`'s layering rule (repositories are Supabase-calls-only). Added `usersService.getOrCreateUser` to compose the repository lookup with a new `clerkService.getUserEmail` call and cache the result on `users.email`; `tasksController` now calls `usersService` instead of `usersRepository` directly. `usersRepository.findOrCreateByClerkUserId` was removed (not kept as a deprecated wrapper) since nothing calls it anymore.
- **2026-08-19, Phase 17:** `TASK_INCOMPLETE` (MISSED→INCOMPLETE resolution) was explicitly confirmed with the user as in-scope before implementation, even though it wasn't in the original prompt's literal example list — flagged as an open judgment call in `PHASE_17.md`, resolved via `AskUserQuestion` rather than assumed.
- **2026-08-19, Phase 17 test-isolation gap (disclosed, not solved):** the new cross-user scheduler test intermittently over-counted `sendEmail` calls because `runNotificationSweep` can, in principle, also sweep another concurrently-running test file's overdue tasks against this project's still-shared (no dedicated test project, per the Phase 7 decisions-log entry) live Supabase project. Fixed the test itself by scoping its assertion to the task's own `notification_log` row via direct query instead of a raw mock-call count; the underlying shared-DB tension remains, same as the Phase 7 note.
- **2026-08-19, Phase 19:** Auto-close-on-leaving-ACTIVE (vs. leave-the-session-open-and-treat-status-off-ACTIVE-as-an-implicit-stop) was the one decision `PHASE_19.md` §5 flagged for explicit confirmation before implementation — asked via `AskUserQuestion`, user chose auto-close. Session rows always carry a real `end_reason`, never a permanently-`RUNNING`-looking row against a task nobody can work on anymore.
- **2026-08-19, Phase 19, real bug found and fixed (not scope creep):** `tasksService.maybeTransitionToMissed` wrote `status: 'MISSED'` unconditionally after reading the task, with no guard against the status having changed since that read — a concurrent Complete/Delete landing first could be silently overwritten back to `MISSED`. Pre-existing since Phase 6, but latent enough to essentially never surface until Phase 19's extra `autoCloseOpenSession` Supabase round-trip (added to `completeTask`/`deleteTask`) widened the race window enough to intermittently fail pre-existing, unrelated tests under full-suite parallel execution. Fixed with a new guarded repository call, `tasksRepository.transitionToMissedIfActive` (adds `.eq('status', 'ACTIVE')`, returns `null` instead of throwing when the row no longer matched so the service can re-read the task's real current state). Confirmed root cause by running the suite with the new `workSessions.test.js` excluded (clean 137/137) vs. included (intermittent MISSED-overwrite failures in `tasks.lifecycle.test.js`, a file Phase 19 never touched) before the fix, then clean/near-clean repeated runs after.
- **2026-08-19, Phase 19, scope boundary (disclosed, not silently expanded):** auto-close was wired into exactly the three call sites `PHASE_19.md` names — `completeTask`, `deleteTask`, `maybeTransitionToMissed` — not into the two bulk missed-sweep paths (`POST /api/tasks/sweep-missed` and the notification scheduler's cross-user `sweepAllUsersMissed`), which write directly via the repository and bypass `maybeTransitionToMissed` entirely. A session left open on a task swept through either bulk path is not auto-closed by this phase. Worth revisiting before Phase 21 (planned-vs-actual analytics) if it turns out to matter for that data.
- **2026-08-18, Phase 16:** Added `client/public/manifest.webmanifest` and `src/components/ui/OfflineBar.jsx`/`src/hooks/useOnlineStatus.js` as real (not speculative) fixes to two `DESIGN.md` §9 checklist items that no prior phase had ever addressed, found by walking the checklist item-by-item as the phase prompt requires — not new-feature scope creep, since §9 has been a standing, already-approved acceptance bar since Phase 9. The manifest ships with an SVG-only icon (`favicon.svg`) rather than generated PNG/maskable icons, since no image-rasterization tooling is available in this environment — logged as a known partial-fix, consistent with the same raster-icon gap flagged (and still unresolved) since the pre-Phase-9 "Brand mark re-imported" entry above.
- **2026-08-21, Deployment/infra (no code changed, no phase involved): backend host switched from Render to Railway.** The user moved the deployed Express backend from `chase-y9n1.onrender.com` (Render) to a new Railway service (project `observant-love`, service `chase`, `production` environment, GitHub-connected for auto-deploy from `main` — the same push flow that already existed for Render). No application code changed for this; it's purely a hosting-provider swap. The frontend's deployed `VITE_API_BASE_URL` (Vercel) will need to be repointed at the new Railway URL once it's confirmed stable — not yet done as of this entry, since debugging below took priority. Locally, `client/.env.local` (gitignored, not committed) was added pointing `VITE_API_BASE_URL` at `http://localhost:8080` for local frontend+backend dev, without touching the committed `client/.env` (which still points at the old Render URL — needs updating to Railway once the swap is confirmed final).
- **2026-08-21, Deployment/infra: email delivery broke after the Render→Railway switch — root cause was environment variables, not code.** `server/.env` is gitignored and was never a deploy artifact for either host, so every env var has to be re-entered by hand in each new host's dashboard; this was already true for Render and just repeated for Railway. Diagnosed by directly querying `notification_log` (read-only, via a temporary local script using the existing `SUPABASE_SERVICE_ROLE_KEY` — no schema/data changes) rather than guessing, since `notificationService`'s whole design already logs a real `error_message` per failed attempt. Root cause evolved through three distinct states, in order: (1) Railway's initial variable set omitted `RESEND_FROM_EMAIL` entirely → every `EMAIL`-channel row failed with `emailService.js`'s own `"Missing RESEND_FROM_EMAIL in environment"` guard (matching `PUSH`-channel rows for the same events, which succeeded, since `VAPID_*` was present) — fixed by adding the variable. (2) Once added (initially as the same sandbox value `Chase <onboarding@resend.dev>` from local `server/.env`), sends started reaching Resend but were rejected with Resend's own `403`: *"You can only send testing emails to your own email address... verify a domain... and change the `from` address."* — `onboarding@resend.dev` is Resend's shared testing sender, which can only ever deliver to the email the Resend account itself is registered under, never to another user's inbox (confirmed directly against Resend's own request logs, not just the error string, via a screenshot of `resend.com/logs/...` showing the exact rejected request). This is a real, permanent constraint of the sandbox domain, not a config mistake — it was already going to block the two-friends/multi-user use case even before the Railway switch surfaced it. (3) User verified a real domain (`waveswebstudio.in`, confirmed `Verified` in Resend's Domains tab) — the fix still requires **also** updating `RESEND_FROM_EMAIL` on Railway to an address on that domain (e.g. `Chase <notifications@waveswebstudio.in>`); verifying the domain alone does nothing to the `from` address already configured. As of this entry the domain is verified but `RESEND_FROM_EMAIL` on Railway had not yet been confirmed updated to it — **first thing to check when resuming this thread:** re-query `notification_log` (channel `EMAIL`, most recent rows) after a fresh task action from a non-`tarunvalluri3@gmail.com` account to confirm `status: 'SENT'` with no `error_message`, which would close this out. No `CLAUDE.md`/`PHASE_*`/`prompts/*` files needed updating for any of this — it's pure ops/config, no lifecycle, API, or data-model behavior changed.

---

## Next Step

**The backend milestone (Phases 0–8) is complete.** `server/API.md` documents the full API contract; `npm test` (in `server/`) runs 73 passing tests; a final manual lifecycle walkthrough confirmed everything works end-to-end against the real app and real Supabase.

**Phase 9 (Frontend Foundation) is done.** `client/` is a working Vite + React + Tailwind v4 scaffold with the full `DESIGN.md` token set, self-hosted Geist fonts, routing, `ClerkProvider`, an API client that successfully calls the live backend, and a `Button` component covering all variants/sizes/states.

**The brand mark is restored** — `client/public/brand/` holds the real light/dark mark and lockup SVGs again, re-sourced from the original Claude Design project, and `client/public/favicon.svg` is the real mark-on-badge favicon (already linked from `client/index.html`). Nothing outstanding from the Phase 9 incident.

**Phase 10 (Authentication Frontend, expanded to include a public landing page) is done.** `/` shows a real marketing landing page (`Landing.jsx`) to signed-out visitors and the authenticated placeholder (`Home.jsx`) to signed-in ones; `/login`/`/signup` are themed Clerk `<SignIn>`/`<SignUp>` screens on an indigo-wash backdrop; `/profile` is a genuinely protected route with working sign-out. Also fixed an app-wide bug from Phase 9 (an unlayered CSS reset was silently zeroing every Tailwind spacing utility) — see the decisions log above; worth a quick visual sanity-check of `Button` and any other Phase 9 UI next time they're touched, since their spacing was quietly wrong until now.

**Phase 11 (Application Shell) is done.** Every authenticated screen (`/`, `/tasks/active|missed|completed|incomplete|deleted`, `/insights`, `/profile`) now renders inside a consistent `AppLayout`: `AppBar` (title + mono date/status context line), `FilterRow` (scroll-snapping status pills, shown only on `/tasks/*`), and the always-visible five-slot `BottomNav` (indigo `layoutId` pill, raised Steel Teal Create action opening a minimal placeholder sheet, amber needs-review dot — badge and filter counts stubbed at zero pending Phase 12's real data). No hamburger/drawer anywhere. See the decisions log above for the one known trade-off (nav pill snaps instead of animating on the specific `/` → `/tasks/*` transition, since `Home` reaches `AppLayout` via `Root.jsx` rather than the shared nested-route instance).

**Phase 12 (Task Management UI) is done.** The five task sections now show real data (`GET /api/tasks?status=...`) via `TaskCard`/`TaskList`, each with loading/empty/error/loaded states and pull-to-refresh; a task detail view (`/tasks/:status/:id`) shows full task info including missed/incomplete/deletion reasons; editing works for ACTIVE tasks only via the real `Sheet` primitive + `TaskForm`; creating a task works from the bottom nav's Create action (and from the Active tab's empty state) through the same form/sheet. Priority is monochrome (rail + label, no hue); status chips use the correct 14%-tint variants including the MISSED→"Needs review"/INCOMPLETE→"Not done" copy. See the decisions log above for the date-input and priority-control choices, and the Phase 12 entry above for the one real bug the `impeccable` detector caught (a height-animating pull-to-refresh indicator, fixed to use `transform` only).

**Phase 13 (Task Lifecycle UI) is done.** Complete, Delete, and Resolve now work end-to-end from both the task card and the detail view: Complete plays the full `DESIGN.md` §5.4 flourish (press → green wash → checkmark draw → exit) before the card leaves its list; Delete and a MISSED→INCOMPLETE resolution get the sober 200ms fade instead; a MISSED→COMPLETED resolution reuses the same flourish, since "the animation itself records the verdict." All three actions are optimistic (UI updates play out immediately, backed by a shared `useTaskLifecycle` hook) and roll back with an error toast if the server rejects. See the decisions log above for the shared-hook architecture and the one logged trade-off (the detail page's action buttons pause for the same animation-duration window as the card, even though nothing is animating there).

**Phase 14 (Dashboard) is done.** `/` now shows a real dashboard: per-status counts (`StatusCounts`/`StatTile`, mono `display`-scale numerals, `tabular-nums`, 2-up grid), a "Due soon" heads-up for ACTIVE tasks with a deadline inside the next 24h (amber, never red), and a "Recent activity" list of the most-recently-touched tasks across every status — all computed client-side (`lib/taskStats.js`) from one unfiltered `GET /api/tasks`, per the phase's explicit "no new backend aggregation endpoint" scope. All four `DESIGN.md` §7.1 states implemented, including a stat-grid skeleton. See the decisions log below for the client-side-vs-endpoint aggregation choice.

**Phase 15 (Analytics) is done.** `/insights` now shows real analytics: completion/incomplete/deleted trends over the last 8 weeks, reason breakdowns for incomplete and deleted tasks (ranked by frequency), a priority-level not-done breakdown, and a live "Needs review" list of unresolved MISSED tasks — all computed client-side (`lib/analyticsStats.js`) from the same unfiltered `GET /api/tasks` Phase 14's dashboard already fetches, rendered via Recharts fully retokenized against `DESIGN.md`'s existing palette (no new categorical hues — see the decisions log above). `DESIGN.md` §7.4 documents the chart design system. All four §7.1 states implemented.

**Phase 16 (Frontend Testing & Polish) is done — the initial 0–16 roadmap is now complete.** `client/` has a real Vitest + React Testing Library suite (87 tests, `npm test` inside `client/`) covering component rendering, the DESIGN.md §7.3 lifecycle-affordance rules, integration-style flows against a mocked API (create/edit/complete/delete/resolve-both-ways/filter/dashboard/insights), and auth-flow behavior. A full `DESIGN.md` audit found zero hex-value drift, zero copy-rule violations, and one real tap-target bug (`FilterRow` pills under the 44px floor, fixed). A walk of the §9 mobile-production checklist found and fixed two previously-unaddressed gaps (no offline indicator, no web app manifest — both added). `npx impeccable detect` returns only the pre-existing, already-approved `overused-font` advisory, unchanged. See the Phase 16 entry above for the full breakdown, including the one item flagged rather than force-fixed (a wording tension between §3.3 and §8 over how much detail a toast announcement should carry).

**Everything scoped in the original roadmap (Phases 0–16) is built, tested, and documented.**

**Phase 17 (Notifications & Email) is approved and Done.** Server-triggered transactional email via Resend now covers task creation, completion (both `ACTIVE→COMPLETED` and `MISSED→COMPLETED`), the `MISSED→INCOMPLETE` verdict, deletion, and meaningful deadline/priority edits — all as immediate side effects of the existing `tasksService` methods, isolated so a Resend failure can never affect a task operation's HTTP response. A new scheduled sweep (plain `setInterval`, no queue infrastructure) is the sole source of missed-task and upcoming-deadline reminder emails, deliberately decoupled from the pre-existing lazy per-request `ACTIVE→MISSED` check. See the Phase 17 entry and decisions log above for the full architecture, the restart-safe dedup mechanism, and one disclosed (not solved) test-isolation gap. Full backend suite: 103/103 passing. **`example.env` has the new vars, but real `RESEND_API_KEY`/`RESEND_FROM_EMAIL`/`CLIENT_URL` values still need adding to `server/.env` before real emails will actually send** — until then, every notification safely no-ops into a `FAILED` `notification_log` row rather than breaking anything.

**Phase 18 (Push Notifications & In-App Feed) is approved and Done.** Web Push plus an in-app notification bell/feed now cover "notifications when the app is not opened." See the Phase 18 entry above for the full architecture.

**Phase 19 (Time Tracking) is approved and Done, backend and frontend both.** `work_sessions` (segment-based, one row per continuous work stretch) backs six endpoints nested under a task (`/api/tasks/:id/sessions/{start,pause,resume,stop}`, `GET /sessions`, `GET /sessions/summary`). Auto-close on leaving `ACTIVE` (the one flagged backend judgment call) was confirmed with the user and implemented; a real pre-existing race condition in the lazy `ACTIVE→MISSED` check was found and fixed along the way. Full backend suite: 157/157 passing (one already-disclosed, shared-test-DB flake class remains, unrelated to this phase). The frontend (`TimeTracker` on `TaskCard` + `TaskDetail`, a live-ticking timer, total-only on the detail page) was added at the user's explicit request right after the backend-only close, with placement/behavior confirmed via `AskUserQuestion` first. Full frontend suite: 112/112 passing.

**Phase 20 (Productivity Analytics 2.0) is approved and Done.** `/insights` is now backed by a new server-aggregated `GET /api/analytics/summary?range=` endpoint (`analyticsService`, kept separate from task CRUD) instead of Phase 15's client-side aggregation — see the Phase 20 entry above for the full metric/chart list and the server-side-vs-client-side decision (confirmed via `AskUserQuestion`). A real, disclosed desktop bug was also found and fixed in the same session: the create-task sheet lived inside `BottomNav`, which is hidden entirely at the `>=960px` desktop breakpoint, so it was unreachable there once a user had any tasks — fixed by hoisting the sheet to `AppLayout` and adding a "New task" button to `Sidebar`. Full backend suite: 169/169 passing; full frontend suite: 126/126 passing.

**Deployment/infra, same session, not a phase:** backend hosting moved from Render to Railway (see decisions log above) — no code changes, but env vars had to be re-entered by hand on the new host, which broke email delivery for a while. Root cause fully diagnosed via direct `notification_log` queries: missing `RESEND_FROM_EMAIL` → fixed, then Resend's sandbox-sender restriction (`onboarding@resend.dev` only delivers to the Resend account's own email) → user verified a real domain (`waveswebstudio.in`) in Resend and updated `RESEND_FROM_EMAIL` on Railway to an address on it. **Confirmed resolved:** re-queried `notification_log` after the user's own fresh test — a `TASK_COMPLETED` `EMAIL` row landed `status: 'SENT'` with no error, and the user independently confirmed receiving the email. Still open, lower priority: the deployed frontend's `client/.env` (Vercel) still points `VITE_API_BASE_URL` at the old Render URL and needs repointing at the new Railway URL once Railway is confirmed stable long-term.

Any further work beyond Phase 20 — a frontend for time tracking, planned-vs-actual analytics (`PHASES.md`'s future Phase 21), Projects, Subtasks, Categories, or anything else from `PHASES.md`'s remaining future list — still needs its own newly drafted phase prompt and the same explicit approval process.
